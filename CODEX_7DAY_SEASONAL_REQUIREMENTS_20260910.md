# Codex向け実装指示書：連続7日運用・四季別日射量・予報不確実性への拡張

作成日：2026-09-10（日本時間）  
対象：`CYLIU2003/master-course`  
用途：学内中間発表・電気学会・修士論文に向けた要件定義、段階実装、検証  
文書版：1.0

> **研究の到達点**：平日・土曜・日曜休日の実ダイヤと日付別PVを用い、同じ車両群・蓄電池の状態を連続して引き継ぐ。季節と低日射の連続が運行成立性・費用に与える影響を説明し、予報の不確実性を含めた運用と設備の選択を比較できるようにする。
>
> 単日の最適化を7回並べること、平均曲線を12枚描くこと、画面に期間選択欄を追加することだけでは完了ではない。

## 0. この文書の位置づけと実行範囲

この文書は実装要件であり、7日間の最適化結果や2025年の実データ納品物ではない。記載する新しい設定項目・成果物名は**提案する論理契約**であり、既存API・クラス・コマンドが既に存在するという意味ではない。実装前に既存の入口、データ型、保存規約を調べ、可能な限り拡張・再利用する。

調査時に参照したコードは `5fac09d9a5775192ab8d3e507c5e72a6fd36afe2`、旧2シナリオの凍結実験は `bb0c0050883a91dd86a9e8813ae88d4b6d8c361d`。作業開始時に現在のHEAD・作業ブランチ・未コミット変更を再確認する。異なるSHAを同じ実験の根拠として混在させない。

本要件作成時には、GitHub上の関連コード・監査文書、東急バスの公式改正告知、Solcastの公式API仕様を確認した。全対象便のブラウザー照合、Solcastの認証付き年間取得、12曲線の実データ生成、solverの修正・再実験は行っていない。`agent-browser`は実行環境に未導入で、`npx --yes agent-browser --version`による起動試行はタイムアウトした。ブラウザー監査済みとは扱わず、以下のWP1で実施する。

既存の `AGENTS.md`、データ保管規約、ローカル検証方針を優先して守る。GitHub Actions、有料APIの購入、有料AIレビュー、外部への投稿、mainへのマージを勝手に実行しない。認証情報はチャット、Git、成果物、スクリーンショット、ログへ出さない。

---

## 1. 最初に固定する事実と、見落としてはいけない問題

### 1.1 既存2シナリオを取り違えない

| 項目 | SUNNY | RAIN |
|---|---|---|
| シナリオID | `771d115b-75b0-49f7-a7f0-25f259a2cd21` | `b23fd26c-1233-4c73-bb9e-bdb8b1584760` |
| 運行日 | 2025-08-05 | 2025-08-05 |
| ダイヤ | WEEKDAY | WEEKDAY |
| PVの由来日 | 2025-08-05 | 2025-08-10 |
| 比較の意味 | 同一平日運行に対する高PV条件 | 同一平日運行に対する低PV条件 |

**RAINは2025-08-10の日曜ダイヤ実験ではない。** 既存の比較は `fixed_weekday_timetable_pv_counterfactual` である。古い文書の別定義を混ぜない。[R2]

旧実験の共通条件は264便・16の内部路線レコード、有効在庫60台（BEV35台・ICE25台）、15分刻み、毎時Rolling、90kW・1ポートの充電器10基、受電上限200kW、PV定格1000kW、BESS6000kWh/900kWだった。16レコードを、確認なしに「営業系統が16本」と説明しない。系統・上下方向・経路パターン・内部IDの対応をWP1で確定する。[R2]

これらは凍結入力の事実であり、現地に実在する設備・公称費用の証拠ではない。また将来の全シナリオに35/25を固定する指示でもない。新ケースの台数・車両IDはFresh Prepareの実効在庫から取得する。旧ICE26台という値へ戻さない。

旧ケースを上書きせず、親ID・親入力ハッシュ・変更差分を持つ新ケースを生成する。初期状態、車両互換性、路線間融通の許可範囲などは、実効入力から引き継ぎ、勝手に緩和しない。

### 1.2 正式な7日実験に先立つSOC問題

2026-09-07/08のリポジトリ監査では、入力SOC上限90%に対し、保存された高PV実行で最大93.375%を検出した。監査文書では3車両・12個の車両×15分境界点の超過と、旧Stage 2が上限を電池容量そのものとしていた問題を記録している。最新READMEは旧比較結果を `DIAGNOSTIC / NOT USED FOR RESEARCH CONCLUSIONS` とし、モデル修正・再実験は未実施としている。[R1,R3]

Codexは現在の到達可能な実行経路で原因を再確認する。古い `VALID`、`24/24`、`OK` を根拠にこの問題を閉じない。Stage 1・Stage 2・Rolling・独立検証のどこで、車両別のmin/max SOCが入力から失われるかを追跡する。解の後処理で90%へ切り詰める修正は禁止する。

WP1の時刻表監査とWP2のデータ整備は並行してよい。ただし、WP0の物理契約是正が完了するまで、7日結果を正式な運行成立・経済性の根拠にしてはいけない。

### 1.3 天候分類の現行実装をそのまま使わない

確認した `src/preprocess/weather/solcast_typical/classify.py` は、日積算CFの三分位（33/67%）または固定閾値で `rainy/cloudy/sunny` を付けている。また発電量が極端に小さい日を除外する処理がある。[R4]

これは日発電量の低・中・高の分類であり、降雨の確認にはならない。冬の晴天を夏の雨天と同じ尺度で分類したり、本当に日射が少ない悪天候を欠損として除外したりする危険がある。既存v1は旧実験の再現用に保存し、新しい気象分類・季節別集計はバージョンを分ける。

### 1.4 土休日のバグは原因を断定する前に再現する

`bff/services/service_ids.py` では `unknown` や未指定値がWEEKDAYへ進む既定処理を確認した。別のloaderにはSAT_HOL関連の別名変換もある。[R5]

これは重点調査対象だが、ユーザーが指摘した生データの土休日バグの原因がこれだけだとは未確認である。原データ→正規化→日付選択→Prepare→実行入力を比較し、最小再現ケースを作る。画面の既定選択と、未知の生データを平日に推測変換する処理を区別する。

---

## 2. 研究の問いと、今回増やさない機能

### 2.1 主たる研究課題

**RQ1：同じ程度の週間PV発電量でも、日射の並び方と平日・土休日の運行需要によって、必要な残量・買電・ICE使用・費用は変わるか。**

日別の発電量だけでなく、低日射の連続、休日から平日への移行、翌朝出庫前の残量が影響するかを検証する。効果が小さい、または見られない結果もそのまま報告する。

**RQ2：予報の誤差や更新を考慮することは、一律の安全余裕や決定論的計画に比べ、運行成立性と費用の両立に寄与するか。**

予報精度だけでなく、SOC不足、未充足便、計画変更回数、計算時間、追加買電・ICE使用を測る。予報を完璧に知っていた場合と実際に使える情報だけで動く場合を分ける。

**RQ3：同じ運行サービスと許容リスクを満たすため、BESS、受電容量、PV、充電器、予備ICE、予報改善のうち何を優先すべきか。**

主な比較軸はBESS容量×受電容量とする。車両・PV・予報品質は段階的な感度分析にし、最初から全組合せを解かない。設備投資と運用改善の代替関係・補完関係を説明する。

### 2.2 新規性の主張範囲

PV・BESS・系統を用いるバス配車／充電、天候比較、PV変動への対応、設備を含む経済性評価自体には先行研究がある。さらに、複数日のバス割当と夜間充電を日次方式と比較する研究も既にある。[L1–L4]

本研究の貢献候補は、**混在車両、日付に対応したダイヤ、日をまたぐ状態、連続悪天候、予報に基づく実行可能な変更を同一の検証基盤で扱い、設備・予備車・運用情報の価値を分離して定量化すること**に置く。「世界初」「7日間だから新規」「確率を使ったから優れている」と記載しない。新規性比較表では、論文に記述がない項目を「未確認」とし、未実施と推測しない。

### 2.3 今回の必須範囲外

V2G、乗客需要からのダイヤ変更、運転士勤務の全面最適化、営業所間の無条件車両交換、系統潮流の全面統合、深層強化学習への置換は追加しない。既存の路線帯固定・所属・互換性を維持する。これらが不可欠だと分かった場合は、根拠・追加工数・研究上の効果を別提案にする。

受電上限を守っただけで、配電線の電圧・熱容量・系統安定性まで保証したとは主張しない。今回の「運用可能性」はモデル化した車両・運行・充電・エネルギー制約内の成立性であり、未モデル化の運転士勤務・故障・交通遅延まで保証しない。

---

## 3. 要件の確定区分

| 区分 | 内容 | 作業時の扱い |
|---|---|---|
| 必須 | 連続7日、各日のダイヤ・PV、車両とBESSの状態引継ぎ、12標準曲線 | 実装する |
| 基準案 | 15分刻み・毎時更新、2025暦年、四季を3か月ずつ | 設定として実装し、根拠と実効値を出力する |
| 要承認 | 日次BESS中立から週次中立へ変える比較、終端条件、分類閾値、計算予算 | 比較仕様を提示し、正式実験前に承認を記録する |
| 外部条件 | Solcastの利用権限・データ再配布、2025当時の時刻表入手 | 確認できるまで該当成果の完成判定を止める |
| 禁止 | データの捏造、欠損の無言ゼロ埋め、SOC初期化、制約緩和の隠蔽 | 例外なく禁止 |

ユーザーの「2025全年」と「2025年度」は一致しない。本書の基準は **2025暦年：2025-01-01～2025-12-31** とする。2025年度を指す場合は **2025-04-01～2026-03-31** を別期間設定として扱う。追加3か月の取得を無断で実行しない。この確認待ちでも、期間を引数にした処理・試験・監査は進めてよい。

---

## 4. WP0：現行経路の調査と、物理契約の是正

### 4.1 実行経路の実査

`AGENTS.md`、`README.md`、`DEVELOPMENT_NOTES.md`、最新blocker文書、保存規約を読み、フロントエンド操作→BFFリクエスト→Prepare→scenario materialization→solver→Rolling→独立検証→最終台帳→画面／帳票の経路を図または表にする。

各段階について、実際のファイル・関数、日付、service_id、PV参照、車両群、SOC上下限、終端条件の受渡しを記録する。コード検索で見つけた旧solverだけを修正して作業完了としない。

`src/objective.py` 等に旧実装が存在しても、それが現在の正式経路かを確認する。凍結 `bb0c005` 専用の報告用監査スクリプトを、一般の7日検証器へ無理に拡張しない。正式契約は本番側の検証器で担保する。

### 4.2 SOC修正の合格条件

車両vの容量C_v、下限率s_v^min、上限率s_v^maxに対し、すべての状態境界と必要なイベント時点で

`C_v * s_v^min <= E_v,t <= C_v * s_v^max`

が成立すること。割合0.90、パーセント90、残量kWhを区別する。初期・終端・運行中・充電後・不使用車両も対象にする。異なるmin/maxを持つ車両を同時に試験する。

独立検証器はsolverと同じ「上限=容量」という誤りを共有しない。元のPrepared車両パラメータと実行軌跡から上限・下限を再構成し、不整合を拒否する。丸め後の表示値で検証しない。

旧シナリオの原本と旧出力を保存し、修正版を新ID／新実験IDで再実行する。修正により配車や費用が変わることは許容するが、旧数値に近づけるためにSOCや費用を操作しない。低PV条件も同一修正版で再実行する。

---

## 5. WP1：東急バスの路線・便・土休日データ監査

### 5.1 路線台帳を先に作る

既存2シナリオの選択route_idから出発し、以下の対応表を作成する。記憶から渋21～23だけを対象にしたり、弦巻の全系統へ勝手に広げたりしない。

`operator_id / depot_id / route_id / 公称系統 / 方向 / 経路パターン / 起点 / 終点 / 停留所列 / 営業距離 / 出入庫区間 / 適用開始日 / 適用終了日 / 出典`

同じ系統でも上下方向、短区間、営業所入出庫、途中始発、別経路を区別する。重複に見える便を時刻だけで削除しない。逆方向や別事業者の便を同一便にしない。

### 5.2 現行公式情報と2025履歴を分ける

公式告知で確認した注意点は次のとおり。

- 2026-09-01改正では、弦巻の渋21・22・23・24について平日・土曜・休日の**途中停留所時刻**が変更され、起終点時刻は変更されていない。[T1]
- 2025-07-01にも弦巻の黒07などで運行回数・時刻の改正がある。[T2]

したがって、現在の公式時刻表と一致しても2025当時の時刻表が正しいことにはならない。逆に、現在と違うだけで過去データのバグと判定してはならない。改正告知だけでは全便の時刻を復元できない。

データの不一致を、(a) 正当なダイヤ改正、(b) 原データ不備、(c) parser／alias／cacheのバグ、(d) 対応する履歴資料がない、に分類する。公式ページで履歴が見られない場合、既存の日時付き原データや権利上利用可能な事業者提供資料を確認する。取得できないときは `historical_timetable_unverified` とし、「2025年実運行再現」の正式ゲートを止める。

最新の検証済みダイヤへ2025年気象を当てる分析は、別の **「固定運行条件に2025年気象を適用した反実仮想」** としてなら設計できる。ただし自動的な代替はせず、利用者の承認と主張範囲を記録する。

### 5.3 agent-browserによる必須確認

公式トップページから、公式リンクの時刻表検索・路線図・改正告知へ移動する。[T3,A1,A2]

```sh
# 既存インストールを確認し、バージョンとヘルプを記録する。
agent-browser --version
agent-browser --help
agent-browser open https://www.tokyubus.co.jp/
agent-browser snapshot -i
# snapshotで実際に見えた参照を使って対象ページを操作する。
# 遷移・日付変更・ダイヤ切替ごとにsnapshotを取り直す。
agent-browser screenshot ./official_timetable_evidence.png
```

これは操作の骨格であり、固定の `@e1` や未確認のURLパラメータを指示するものではない。Windowsで `.ps1` が実行制限を受ける場合は、利用可能な `.cmd` ランチャー等を確認する。端末全体のセキュリティ設定を無断で緩めない。

未導入の場合は公式手順と環境権限を確認する。認証・ネットワーク・サイト側制限で停止したら、そのページと理由を保存する。CAPTCHAやアクセス制限を回避しない。無制限の高頻度収集はせず、キャッシュと再利用を優先する。

各系統／方向／経路パターンについて、平日・土曜・日曜休日をすべて確認する。土曜休日共通のページは、その適用範囲を保存する。始発・終発・全発車便数だけでなく、途中止まり・途中始発、注記、到着／通過時刻の根拠を確認する。発車時刻しか公開されない箇所では到着時刻を実測値と称して作らない。

保存証跡は、取得日時、最終URL、選択した日付／曜日区分、ダイヤ改正日、経路・方向・停留所、画面画像、取得できる原テキスト／HTML、ハッシュ、照合対象の原データ行。監査一覧に `VERIFIED / MISMATCH / UNAVAILABLE / NOT_CHECKED` を必ず付ける。スクリーンショットの枚数だけを完了条件にしない。

### 5.4 日付とサービスカレンダー

日付ごとに適用serviceを解決する。優先順位は、事業者が定める日別例外・特別運行、祝日等の適用、通常の曜日規則とし、最終判断の出典を保存する。年末年始や特別運行を単純な曜日判定で処理しない。

`WEEKDAY / SAT / SUN_HOL / SAT_HOL` の意味を既存契約に合わせて統一する。SAT_HOLを土曜だけに落としたり、SATとSUN_HOLの両方を重ねて同一便を2回生成したりしない。祝日が平日・土曜に当たる場合も試験する。日付別適用はservice_idの文字列を付け替えるだけでは完了しない。

未知・空の原service_idを正式入力でWEEKDAYに推測変換しない。UIで利用者が明示した平日選択は別に記録する。不明と指定済みを同じ値に正規化しない。

### 5.5 時刻表監査の機械検査

以下を日付×系統×方向×パターン×事業者で検査する。

| 検査 | 判定方法 |
|---|---|
| 運行便の網羅 | 原データの運行対象便集合と最適化対象便集合を比較。単なる合計便数一致では不可 |
| 一意性 | 便実体は少なくとも運行日＋事業者＋原便IDで識別し、必要に応じてパターンIDを加える |
| 無運行と欠損 | 公式に運行なしと、ファイル欠損・抽出失敗を区別する |
| 時間単調性 | 停留所順、到着・発車、乗務／回送／折返しの時間整合を確認する |
| 日跨ぎ | 24:10等を運行日から翌暦日へ変換し、先に24時間剰余へ落とさない |
| 距離 | 営業・回送を区別し、欠損・0・仮の直線距離を正式値にしない |
| 車両接続 | arrival＋turnaround＋deadhead≦next departureを維持する |
| 生データ保全 | 原本は変更せず、修正前後・理由・出典・承認・ハッシュを残す |

土休日バグは「壊れた原データfixture→失敗するテスト→最小修正→成功するテスト」の順に修正する。温存されたcacheで偶然通ることを避け、空cacheと既存cacheの両方を試験する。対象日の便数を264へそろえない。

---

## 6. WP2：Solcast年間データ取得と日付・単位の正規化

### 6.1 地点・データ権限

弦巻営業所の緯度経度は、既存の地点レジストリと位置の根拠から確認する。町名の代表点や適当な座標を作らない。対象地点、座標系、座標精度、出典、地点IDを固定する。

Solcastの大学研究向け利用窓口はあるが、現在の利用権限、利用期間、出力項目、履歴予報、再配布権まで自動的に認められるわけではない。[S3] 手元の正規CSVや既存契約を優先して確認する。APIキーは環境変数／既存secret管理から読み、表示・コミットしない。料金や権限が未確認なら追加購入せず、必要項目と停止理由を報告する。

取得するのは2025年のHistorical Time Seriesであり、TMYや現在から先の予報で置き換えない。履歴値は衛星・気象モデル等に基づく推定値であり、営業所の日射計・PVメーターの実測とは区別する。[S1]

### 6.2 API実装の契約

公式に確認した入口は `GET /data/historic/radiation_and_weather`、1リクエストの期間上限は31日である。[S2] 実装時に公式OpenAPIと利用契約を再読し、パラメータ名・応答構造・境界条件を確認する。既存APIクライアントがあればそれを拡張する。

まず1日分を取得し、地点・時刻・単位・必要列・課金範囲を検査する。その後、31日以内の期間へ分割し、既取得区間を再利用して対象年を結合する。任意の例外で全年を最初から再取得しない。429はRetry-After等に従い、401/403は権限問題として停止する。

基本取得項目はGHI、期間終端、期間幅。分類とPV変換のため、権限内でclear-sky GHI、降水強度、weather_type、気温、DNI/DHI、必要なGTIを取得する。API列名の候補は `ghi / clearsky_ghi / precipitation_rate / weather_type / air_temp / dni / dhi / gti`。利用可能性・単位は実際の応答と公式定義で確認する。[S2,S4]

GTIは傾斜・方位・設備種別等の条件を明示する。SolcastとPV変換ライブラリの方位角規約が同じと仮定しない。必要な気象列が取得できなければ、日射量のみの取得完了と、気象3分類の未完了を分けて報告する。

### 6.3 時刻・期間の仕様

基準期間はJSTの半開区間 `[2025-01-01 00:00, 2026-01-01 00:00)`。UTCでは `[2024-12-31 15:00Z, 2025-12-31 15:00Z)` に対応する。15分値を取得でき、欠損がなければ365日×96＝**35,040区間**になる。

Solcastの `period_end` は期間終端、`period` は期間幅として扱い、始端を差し引いて求める。提供側の包含・除外規則を1日テストで確認し、取得境界の重複・欠落を検査する。UTCの暦日で分割したままJSTの日射曲線にしてはならない。

原本のタイムスタンプは保持し、正規化後はtimezone-awareなAsia/Tokyoの始端・終端・期間秒を持つ。既存の「終端から1秒引いて日付判定」等がある場合も、区間の重なりによる割当と等価かを試験する。日付だけを変えた再利用を暗黙に行わない。

### 6.4 単位・エネルギー保存

GHI/GTIのW/m²と、積算日射量のkWh/m²を別フィールドにする。W/m²の区間平均Gと時間幅Δt[h]から積算日射量は `G * Δt / 1000`。降水強度mm/hも区間幅を掛けて積算する。cloud_opacityを雲量そのものとして扱わない。[S4]

時刻刻みを変える場合は、区間の重なりに基づく積分量保存を行う。1時間データを15分に展開しても、15分の観測変動を復元したとは扱わない。native cadence、resampled cadence、補間法を保存する。

欠測、夜間の正常な0、データ品質異常を分ける。昼間欠測を0とみなさない。低発電であることだけを理由に雨の日を除外しない。正式な週に必要な区間が欠損すれば、その週は不完全データとして停止する。補間ケースは別の感度分析に限定し、補間率と方法を表示する。

### 6.5 日射量からPVへの変換

次の3段階を混ぜない。

`気象・日射量原本 → 設備条件を固定したPV利用可能出力 → 最適化が選ぶPV利用・抑制`

GHIをそのまま傾斜面日射量とみなさない。変換器は設置傾斜、方位、DC容量、AC上限、温度・損失・効率の扱いとバージョンを持つ。既存の簡易変換を残す場合は「PV推定proxy」とし、別途妥当性・感度を示す。取得したPV電力列もkW/MW、AC/DCを確認する。

季節間で容量・効率を勝手に変えて発電量を調整しない。降雨に対する追加係数などを二重に掛けない。設備出力上限によるクリッピングはモデルとして明示し、誤った入力や単位の問題を隠す汎用clipにしない。

既存PV1000kW/BESS6000kWhの仮定はまず保存する。設置面積・費用を含む現実的な設備設計はWP5の別ケースで扱う。[R2]

---

## 7. WP2：四季×3天候、12標準曲線の作成仕様

### 7.1 季節と分類対象

季節は春3–5月、夏6–8月、秋9–11月、冬12–2月とする。2025暦年の冬は1–2月と12月を集計するが、これを連続した同一冬の運行期間として接続しない。

「天候」の定義を明文化する。本研究の主目的に合わせた**日中の日射条件を表す研究用分類**と、提供元のweather_type／降水情報を両方保存する。公式の日天気概況そのものと称さない。

### 7.2 分類規則の基準案

以下は実装可能な初期案であり、気象機関の公式定義ではない。正式集計前に、適用例と境界日の一覧をユーザーへ示し承認を記録する。

日中集合D_dは、太陽高度またはclear-sky日射の閾値で定義する。夜間の `CLEAR` 等を昼の晴天根拠に混ぜない。閾値と日中判定法を出力する。

`H_d = Σ(t∈D_d) GHI_d,t * Δt / 1000`

`H_clear,d = Σ(t∈D_d) clearsky_GHI_d,t * Δt / 1000`

`K_d = H_d / H_clear,d`

`R_day,d = Σ(t∈D_d) precipitation_rate_d,t * Δt`

基準案は、日中積算降水量R_day,dが1.0mm以上なら雨、未満かつK_dが0.65以上なら晴れ、それ以外はくもりとする。境界値の以上・未満を固定する。雨量の0.5/1/2mm、Kの0.55/0.65/0.75を感度確認に用い、都合の良い閾値へ後から調整しない。

夜間だけの降雨は夜間降雨フラグで保持し、「日中は晴れ、夜間に雨」のように説明可能にする。weather_typeとの矛盾、降雪・積雪、強雨、データ異常は補助ラベルと監査表へ残す。雪を無条件に雨へ変換せず、分類不能なら保留する。公表天気の晴れ・くもり・雨を求める場合は、その日別ラベルの出典と対応規則を別契約にする。

降水データが欠測している日に0mmを仮定しない。Solcastの降水が格子推定値の場合は地点実測の雨量と区別する。追加の観測所データを利用する場合は、観測地点・距離・期間・品質・対象地点との違いを記録し、別ソースへの変更を黙って行わない。

### 7.3 出力する曲線と付帯情報

季節×天候の12区分ごとに、まず**GHIの15分平均標準曲線**を必須出力する。設備面のGTIとPV[kW]の標準曲線は、設備条件を固定した派生物として別に作る。日射量とPVを同じ列や同じ単位で表さない。

各区分に必要なデータは次のとおり。

| データ | 用途 |
|---|---|
| 区間平均、中央値、q10、q90、標準偏差 | 中心と日ごとの分布を説明する |
| 日積算日射量・日PV量の分布 | 瞬時の帯と日全体の変動を分ける |
| 使用日数、全使用日付、除外日と理由 | 小標本・選別による偏りを確認する |
| 品質、native cadence、分類規則、閾値、地点、設備条件 | 再現する |
| 原データ・分類・変換・学習分割のハッシュ | 来歴と情報漏洩を検査する |
| 実在する代表日（medoid等）の曲線 | 平均化で失われる日内の変動を補足する |

同じ96区間・同じ重みの有効日から平均を作った場合、平均曲線の積分が対象日の平均日積算量と一致することを試験する。中央値曲線について同じ恒等性を要求しない。

q10≤q50≤q90を検査する。ここでqは下側から数える経験分位点と明示し、他製品のP90等の超過確率表記と混同しない。区間別のq10–q90帯は、1日全体・1週間全体が80%の確率でその中に入る保証ではない。

空区分を0曲線や他季節の流用で埋めない。使用日数0は `INSUFFICIENT_DATA`、10日未満は小標本警告を出すという基準案を採用する。少数日しかない区分の確率推定や裾の議論を強く主張しない。12区分のファイルが存在することと、12区分すべてが十分なデータを持つことを別々に判定する。

### 7.4 平均曲線を実日の代わりにしない

実在する7日間の評価には、その7日間の利用可能PV系列を使う。標準曲線に置き換えた実験は代表条件実験として別IDにする。

標準曲線を異なる日付の予報proxyに適用する場合、日の出・日の入りの違いで夜間発電を作らない。clear-sky日射に対する減衰の形を対象日へ移す方法などを検討し、時間変形、積算量、変換誤差を検証する。単純に夏の時計時刻曲線を冬へ貼り付けない。

---

## 8. WP3：連続複数日に必要なデータ契約

### 8.1 時間の識別を分離する

少なくとも以下を別々に扱う。

| 概念 | 意味 |
|---|---|
| service_date | 便が属する運行日 |
| departure_at / arrival_at | timezone-awareな実際の暦日時刻 |
| weather_valid_at | 日射・PVが対応する時刻 |
| forecast_issued_at | 予報が発行された時刻 |
| decision_at | 最適化に使える情報の締切 |
| timestep_start / timestep_end | 電力・燃料等の実行区間 |

主な評価窓はJSTの連続168時間、15分なら672区間・673状態境界とする。深夜便の完了や評価開始前から続く運行がある場合、初期状態とtail処理を別途持つ。7日目の24:10便を落として「7日充足」としない。運行日で対象便を選ぶ契約と、暦時刻で電力量を集計する契約の対応を表示する。

### 8.2 期間入力の論理契約

新しい多日ケースは、期間、親ケース、選択route_id、日付別service、時刻表版、PV原本、料金、車両実効在庫、初期状態、終端方針、予報情報、解法制御、変更差分をまとめた変更不能な入力束を持つ。

以下は新規フィールド候補であり、未確認の既存API名として使ってはならない。

```yaml
period:
  start_at: <timezone付き開始日時>
  end_at: <timezone付き終了日時・排他的>
  timezone: Asia/Tokyo
  slot_minutes: 15
  execution_step_minutes: 60
source_context:
  parent_scenario_ids: [<既存ID>]
  timetable_mode: historical_replay | fixed_service_counterfactual
  weather_mode: historical_estimated_actual | representative | synthetic_stress
calendar:
  daily_manifest: <日付ごとのservice・時刻表版・対象便hash>
fleet:
  active_contract_hash: <Fresh Prepareの実効車両集合>
  initial_state_hash: <最初の1回だけ与える状態>
terminal_policy:
  bess: daily_neutral | weekly_neutral
  bev: <車両別の終端残量・翌日成立条件>
forecast:
  source_kind: archived_issued | climatology_proxy | synthetic
  training_split_id: <識別子>
  issue_time_policy: <利用可能性の契約>
```

Prepareのcache keyには対象期間、日別カレンダー・時刻表版、PV・予報、車両契約、設備・料金、変換器・schema版を含める。同一時刻の「08:00」だけ、同じscenario_idだけ、同じweekdayだけで別日の入力を再利用しない。

### 8.3 複数日で引き継ぐ状態

BEVごとの残量、BESS残量だけでは不足する。ICE燃料、車両位置、走行中の便・残り運行、回送中の状態、充電器接続と残り拘束時間、車両使用可否、日別使用判定、請求期間の既知ピーク等も、次の実行判断に影響する範囲で引き継ぐ。

同じ車両IDを日ごとに新しい車として生成しない。運行から戻っていない車を0時に営業所へ戻さない。整備・故障による利用不可は理由を記録し、期間中に黙って車両を削除・追加しない。

---

## 9. WP3：最適化モデルの必須制約

### 9.1 便充足と時間・位置

各対象便をちょうど1台が担当し、日付・事業者・経路を含む便集合を完全に充足する。運行時刻は固定する。到着後の折返し・回送を含む接続条件を維持する。

営業距離と回送距離を別々に電費・燃費へ反映する。位置が異なる場所で次便を開始するには、実行可能な回送が必要である。24時をまたいでも同じ条件を使う。営業所・路線帯・事業者・車両種の互換性は既存の実効契約を維持する。

### 9.2 BEV・BESS・ICEの連続状態

BEVの概念式は次のとおり。既存の走行イベント方式・区間方式のどちらを使うかは現行実装との整合で選び、二重控除しない。

`E_v,t+1 = E_v,t + eta_v * P_charge_v,t * Δt - E_trip_v,t - E_deadhead_v,t - E_aux_v,t`

BESSは

`B_t+1 = B_t * (1 - loss_t) + eta_ch * P_bess_ch,t * Δt - P_bess_dis,t * Δt / eta_dis`

とし、許容残量、充放電電力、同時充放電禁止を守る。損失をモデル化しない場合は0と明示する。同時充放電を緩和するなら、目的関数が排除するという推測だけでなく数学的条件と独立検査が必要で、基本は明示的に禁止する。

ICEは

`F_v,t+1 = F_v,t + refuel_v,t - fuel_consumed_v,t`

とし、タンク上限・下限と給油の場所・時間・同時処理能力を扱う。毎朝144Lへ戻す、無料で補給する、運行中に給油する実装は禁止。給油設備の実データが不足する場合は仮定を明示し、承認された給油方針の下での成立性とする。

### 9.3 電力収支と充電器

営業所の共通電力基準点で、系統購入＋利用PV＋BESS放電＝BEV充電入力＋BESS充電＋建物等負荷を満たす。利用可能PV＝利用PV＋抑制PVも成立させる。各機器のAC/DC・効率位置を明示し、損失を二重計上しない。

受電上限、充電器定格、車両側最大充電電力、ポート数、接続互換性を同時に守る。15分枠の一部しか停車していない車へ15分全体の充電を与えない。時間離散化が緩和する範囲を明示し、充電可能時間に基づくエネルギー上限とイベント整合を検証する。

「充電電力が正の車両数」と「ポートに接続している車両数」は別集計にする。微小電力を0表示にしてポート違反を隠さない。現行非双方向条件を維持する。

PV→バス、PV→BESS、系統→バス、系統→BESS、BESS→負荷、抑制を保存する。BESSからの供給をすべてPV由来と称してはならない。由来を追跡する場合は初期BESSの由来、損失、在庫を含む会計規則を設ける。車両別の比例配分はsolverが直接決めた電源帰属とは区別する。

### 9.4 日跨ぎと終端条件

同じ境界時刻の前後で状態を引き継ぐ。例えば `E_v,d,24:00 = E_v,d+1,00:00`。等式は初期化命令ではない。夜間充電とその費用・受電制約を実際に解く。

BESSについて、過去の「一日の残量差は0が望ましい」という指導を勝手に削除しない。[R3] **日次中立**を基準比較として保存し、**週次中立**を日間融通の効果を測る別ケースにする。週次中立でも7日最終残量を初期残量等の固定目標へ戻し、初期在庫の使い逃げを許さない。

BEVは車両ごとの終端残量条件を持たせる。基準案は、所定の終端時点で初期残量以上を確保し、次の運行日の初便までの回送・充電・必要残量も成立すること。単なる車群合計一致で、特定の車両だけを空にしない。初期状態が低SOCなら翌日の準備条件を併用する。

終端を毎日、毎Rolling窓、週末のどこに課すかを混同しない。ルックアヘッド窓の終端処理は各手法で同条件にし、8日目の準備や末尾便を扱うための参照期間・費用も明示する。追加期間のデータがなければ完全予見であるかのように補わない。

最終在庫の金額評価を採る代替案は、物理的な翌日運行の条件と併用する。在庫評価額を払えばSOC下限や次便必要残量を破ってよいわけではない。

---

## 10. WP3：7日一括計画とRolling実行を区別する

### 10.1 7日一括の決定論的計画

672区間を同時に扱い、日をまたいだ車両・BESS・給油等を連結する。歴史的なPV全系列をあらかじめ渡すケースは**完全予見の参照計画**であり、当時可能だった実運用予報ではない。

大規模問題が時間内に最適性証明まで到達しない場合も、検証を通った可行解、下界、gap、打切り理由を保存する。「7日一括」でも候補限定のPhase 3なら統合大域最適解ではない。小規模の1–3日問題では完全な統合モデルや列挙可能な参照問題と比較して近似誤差を確認する。

### 10.2 7日間の閉ループRolling評価

評価期間は7日、意思決定の更新は60分を基準とする。毎回の最適化は設定したlookahead（24/48/72/168時間等）を用い、**先頭60分だけを実行**する。残りの予測計画を実行済みデータへ加算しない。

評価窓がちょうど168時間なら、168回の実行区間と672個の15分実行枠を持つ。日次解7本や24回分の合格印の使い回しでは不可。各時刻で実行状態・予報発行時刻・入力hash・solver状態・実行hash・費用を保存する。

#### 予報と実現値が違うときの物理的な電力追随

毎時の計画更新と、PV変動に追随する電力制御を別の層にする。実現PVが予報と違っても、計画の買電・BESS・充電をそのまま記録して電力収支が成立したことにしてはならない。

全比較手法で同じ、事前定義した追随則を使う。例えば、確定した充電要求に対するPV不足を、受電余力・BESS出力／残量の範囲内で補う。超過PVの蓄電・抑制も事前に定義する。どちらを優先するかを設定として固定し、追加のエネルギー・費用を実行台帳へ記録する。利用できる資源で補えなければ不足として記録し、架空の系統購入や事後SOC修復を行わない。

追随判断にはその時点で観測できたPVと残量だけを使い、次の区間の実現値を与えない。15分平均値による追随の模擬は、その時間解像度の準静的近似とし、秒単位の電力品質保証とは区別する。これは宣言した運用方針の評価であり、solverの不正解を隠すpost-solve repairではない。

既に出発した便、進行中の回送、接続拘束中の充電、直近の確定運行は変更しない。未出発便をどこまで変更可能にするか、何時間前に車両割当を確定するかを契約化する。比較手法間で同じ確定方針を使う。変更した車両割当数、充電開始時刻の変更、運行担当への再通知回数を記録する。

再最適化は、上記の電力追随を含む実行済み状態を使って次を解く。後から判明した日射の実現値を、過去の予報へ書き戻さない。停止後の再開では最後に受理された状態から再開し、区間の重複実行・二重課金を防ぐ。

### 10.3 計算量と解法の扱い

最初から7日×多数気象シナリオの巨大問題へ飛ばない。1日回帰、2–3日の連結、7日の決定論、7日のRolling、確率的拡張の順に通す。ボトルネックを前処理、Stage 1、全候補Stage 2、検証、出力へ分けて測定する。

完全な実行可能successor networkを基準にする。枝刈り・候補数制限・粗い時間刻みを使う比較は、探索範囲と影響を別ケースとして明示する。選択された候補のsolve_timeだけをシステム全体の計算時間と呼ばない。

正式な計算予算は1日・3日・7日の試験後に決め、結果を見る前に固定する。毎時更新では処理完了が次の意思決定締切に間に合うかを検査する。制限時間内の解がない状態を「物理的に不可能」と同一視しない。

---

## 11. WP4：予報不確実性を、単なる天候差と区別する

### 11.1 データの3種類

| 種類 | 内容 | 可能な主張 |
|---|---|---|
| 履歴推定値 | 2025年の時刻別日射／PV estimated actuals | その推定系列下の運用評価 |
| 発行時刻付きの履歴予報 | 当時、将来時刻に対して発行された予報 | 予報誤差と予報を使った運用の評価 |
| 標準曲線・合成予報 | 学習データから作った気候値、分類予測、摂動 | 定義したproxy／仮定下の不確実性評価 |

Historical Time Seriesを取得しても、2025年当時の予報が取得できたことにはならない。現在のforecast APIを呼んでも過去の発行済み予報にはならない。履歴予報の提供・取得権限を別に確認する。[S2]

発行済み予報がある場合、誤差は同じvalid timeについて `実現推定値 − 発行済み予報` と定義し、lead time、季節、時刻で分類する。営業所実測ではなくSolcastの履歴推定値に対する誤差なら、その基準も明示する。

履歴予報がない場合は、学習データのみから作ったclimatology proxy等を使用し、**Solcast予報の精度を検証したとは主張しない**。実際の2025年の晴雨ラベルを先に与えたケースは、天候クラスを既知とした上限的な参照ケースであり、通常の予報実験とは分ける。

### 11.2 全年12曲線と、検証用12曲線の分離

「2025年すべてを使った12曲線」は、全年の記述統計として作成する。同時に、予報評価には学習区間だけから作成した12曲線を別に用意する。

同じ日のデータを平均曲線作成・分類閾値調整・残差分布推定と性能評価の両方に使わない。日内15分枠をランダムに分けるのではなく、連続した日／週を単位に学習・検証・テストを分離する。lookaheadや気象ブロックの長さに応じた分離期間を設ける。

2025年内のblocked cross-validationで、テスト日より未来の別日を学習に使う場合は、回顧的な汎化評価と明記する。実時間利用の厳密な再現では、その意思決定時刻より過去の学習データだけを使う。年初の学習不足を解決するため2024年等を追加取得する場合は別承認にする。

### 11.3 不確実性系列の生成

平均曲線の標準偏差は、同じ季節・天候の異なる日を比べたばらつきであり、予報誤差そのものではない。これを区別したメタデータ・画面表記にする。

不確実性の再現では、15分ごとに独立乱数を加えるだけの方法を主方式にしない。実際の日内変動と連続低日射を保つため、学習区間内の1日または複数日の残差／日射ブロックを使う。季節、日射量、連続悪天候の長さ、時刻間自己相関、ランプ量、日積算量の分布を原データと比較する。

主評価は実在する連続週の外部テストとする。合成系列は追加ストレス試験として保存し、実日付の観測系列と混ぜない。夜間発電、負の日射、設備定格超過などの物理的矛盾を検査する。無条件clipで分布を変えた場合は変更量を記録し、生成法自体を修正する。

晴雨クラスの誤分類と、同じクラス内の日射量・時系列の誤差は別の要因として扱う。どちらか一方しかモデル化しない場合は、その限定を明示する。予報更新時の改善幅や相関を実データなしに「実際の予報性能」として作らない。

### 11.4 不確実性を使う運用方法の実装順

必須の第1段階は、決定論的Rolling計画を、実現PVで閉ループ実行して不確実性の影響を測ること。平均費用だけでなく失敗頻度・不足便・不足残量・追加対応・計画変更を保存する。

提案法の第2段階は、同じlookahead・確定済み配車範囲・更新間隔で、**条件付きの複数PV系列を用いて安全余裕を確保するRolling**とする。既存の候補配車生成とStage 2を再利用し、候補ごとに、同一の直近実行判断が選んだ系列群で実行可能かを評価する。

近似的な2段階確率モデルを採る場合、直近60分の充電予約・充放電の基準計画・許可された配車判断は全シナリオで共通にする。実現PVへの追随は10.2節で定めた共通の制御則に限り、現在観測に対する応答と将来を知る判断を区別する。既に確定した将来配車も共通にする。観測後の再対応だけを分岐させる。シナリオごとに先頭1時間から別の計画を解き、平均費用を実行可能な提案法の費用とすることは禁止する。

将来が一度に判明する2段階recourseは、多段階の実予報更新より楽観的になり得る。その近似を表示し、採用する先頭区間だけを実現系列で逐次評価する。計算量が許せばシナリオ木で、同じ観測履歴を共有する判断に非予見性制約を課す。

リスクの主な制御は、事前に選んだシナリオ集合に対する便・SOC・設備制約の成立とする。期待費用を基本目的とし、CVaRや確率制約を追加する場合は、信頼水準・損失定義・校正・標本数を別に定義する。有限シナリオで成立しただけで、実世界の95%保証や全不確実性に対する頑健性を主張しない。

時間が足りない場合は、第1段階を完成させ、第2段階を未実施として明記する。画面に「ロバスト」を付けただけで完成としない。既存Phase 3のStage 1/Stage 2と、確率モデルの事前／事後の2段階は異なる概念なので用語を分ける。

---

## 12. WP5：比較実験の設計

### 12.1 実験を3系統に分ける

**H：日付整合の履歴再現**。実際の対象日、対応する時刻表版、日別PVを使う。季節の比較にはダイヤ改正・祝日・気温等の違いも含まれるため、差をすべてPVの効果と説明しない。

**C：要因を分離する反実仮想**。同一の検証済み月曜～日曜運行テンプレート・車両・設備・料金等を固定し、各季節のPVや予報条件だけを変える。実在の当該年ダイヤの再現とは区別する。季節の電費影響を調べる場合は、PVだけを変えた結果とは別の因子にする。

**S：合成ストレス**。同じ週のPV日曲線の集合を使い順序だけを入れ替える、連続低日射を強める、予報誤差を増す等。ダイヤを固定したままの順序変更は、天候と曜日の組合せも変えるため、その影響を含む実験と明記する。複数の並びを用い、特定の順序だけで結論を出さない。

C/SをHのデータ不足の無言の代用にしない。すべて実験IDと入力manifestを分ける。

### 12.2 比較手法の最小セット

既存のM0～M3等の名称に意味がある場合はそれを調べ、上書きしない。以下は新実験用の仮のラベルである。

| 仮ラベル | 内容 | 比較する効果 |
|---|---|---|
| X24 | 状態を引き継ぐ24時間lookahead・毎時更新 | 短い先読みの基準 |
| X48 | 同じ入力情報で48時間lookahead・毎時更新 | 日跨ぎを先読みする価値 |
| X48-U | X48と同じ窓・更新・確定範囲で不確実性を考慮 | 予報不確実性対応の価値 |
| X168-PI | 7日全体のPVを既知とする参照計画 | 完全予見の参照。最適性未証明なら厳密下界ではない |

日次BESS中立／週次BESS中立は、上記手法とは別の実験因子とする。固定配車の充電最適化と配車も更新する方式の比較も、別の因子として追加する。必要な比較だけを事前に選ぶ。

毎朝SOCをリセットする方式は、影響を示す診断比較には使えても、実行可能な従来法として主要な費用改善率の分母に使わない。未知の未来PVを使うX168-PIと予報を使うX48の差は、先読み期間だけでなく情報の差も含む。先読み期間単独の効果は同じ情報集合で比較する。

### 12.3 季節ごとの7日間を恣意的に選ばない

初期発表用は各季節につき連続7日を1本ずつとする。単純に最も晴れた週を選ばない。solver結果を見る前に、気象統計・欠損の有無・ダイヤの検証状況から代表週の選定規則を固定する。例えば週間日射量・変動・日射の低い日数が季節内分布の中心に近い、完全な月曜～日曜の週を選ぶ。

別途、検証可能な連続低日射週を各季節で抽出してストレス確認する。代表週と重なる場合はその事実を記録する。予報モデルの学習データと評価週を分ける。計算資源が許せば各季節3週以上へ増やすが、4週だけで年全体・将来の運行信頼度を断定しない。

対象便数は日別に異なる。全日264便を要求しない。平日・土曜・日曜休日、日跨ぎ、月境界、年末特別ダイヤを少なくともテストデータとして含める。

#### 初期条件と気温の切分け

各方式は同一の車両別初期残量・初期燃料から開始する。初期在庫に依存した見かけの成立を調べるため、利用可能データの範囲内で事前運用期間を置く比較、初期残量の感度、14日・28日連続の追加評価を段階的に行う。追加気象データが必要なら取得範囲と権限を確認する。7日成功だけを長期の定常運用の証明にしない。

PVだけを変える主比較では車両電費を固定する。季節の空調負荷・気温による電費変化まで主張する追加比較では、車両に対応する根拠を持つモデルまたは明示的な感度幅を用いる。外気温を取得しただけで、BEVの季節電費が実証できたことにはならない。

### 12.4 失敗も結果として残す

全便充足、SOC・設備制約違反なし、物理・会計・来歴検証合格の実行を正式な成立ケースとする。未充足や容量不足を検査する診断モードは、別の状態で記録する。

`PROVEN_INFEASIBLE`、時間内に可行解が見つからない、実行時の予報誤差で成立しない、データ欠損、実装不具合を分ける。失敗週を母数から削除しない。救済運用を比較する場合は、予備ICEの呼出し、追加給油、配車変更等を事前定義して費用と資源を計上し、隠れたfallback／repairにはしない。

### 12.5 比較の公平性

共通条件は、対象便集合、車両IDと初期状態、設備、料金、出入庫・所属規則、終端処理、確定済み計画、実現系列、評価窓、seed・threads・計算予算である。異なる項目は実験因子として明示する。

候補探索不足による差を天候効果と混同しない。可能な範囲で候補集合の相互評価、複数seed、小規模参照解で確認する。gapが大きい場合、僅かな費用差から手法の優位を断定しない。天候が変わっても配車が同じになる結果は排除しない。

---

## 13. WP5：費用・環境・政策への接続

### 13.1 現行の評価額を実費と呼ばない

凍結パラメータでは `objective_is_actual_cost=false` であり、車両使用費20,000円/台日等のモデル評価項目を含み、需要料金・運転士費・劣化費・PV/BESS設備費は0だった。[R2] この合計をそのまま実際の支払額、年間総保有費用、投資回収期間の根拠にしない。

`solverの探索目的値`、`実行した物理量からの運用費`、`車両使用等の評価費`、`設備の年換算費`、`炭素等の社会的評価`を別々に出力する。費用を合わせるために燃料L、距離km、SOC、電力量kWhを変更しない。

### 13.2 日・週・月の計上を混同しない

車両使用費は、定義上対象となる便をその運行日に1回以上担当した車両について、車両×日ごとに一度計上する。同じ車両を週に1回だけ課金したり、Rollingのたびに課金したりしない。営業日境界をまたぐ便の所属日も定義する。

燃料の消費費用と給油の現金支出を分ける。給油量と消費量の両方に同じ燃料費を加算しない。初期・終端の燃料／電池残量の在庫評価は、全評価期間の境界で一度だけ行い、途中日の調整額は週集計で相殺されることを確認する。

日別台帳の合計が週別台帳へ一致し、週別台帳は実行区間から独立再計算できること。既存の `executed_day_accounting.json` の役割を保存し、多日台帳の正本を設計する。各Rolling窓の未実行部分を合計しない。表示の丸め前に照合し、既存の帳票間整合許容1e-6円を維持する。[R6]

電力基本料金は料金制度の請求期間・平均化窓・契約条件を確認する。週最大電力×月額単価×7/30は、採用する場合も単なる配賦仮定であり、実際の請求額とは区別する。月内の既知ピークが必要なら状態として引き継ぐ。

### 13.3 設備費を含む比較

固定設備で運用を比較した後、PV、BESSのkWh/kW、充電器、受電容量、BEV/ICE在庫を変更する。設備費は容量、出力、工事、維持費、寿命、割引率、交換、残価を区別し、実データか仮定か、価格年、税の扱いを記録する。

車両購入費の年換算額を計上する場合、同じ固定費を旧20,000円/台日と二重計上しない。使用台数の便宜的評価費を残すなら、設備費とは別の指標として示す。乗務員費を省く場合はそのために比較できない範囲を明示する。

初期は「BESS容量×受電容量」の少数の実現可能な候補を比較し、運行成立の境界付近だけ細かくする。最小容量の探索では、solverが時間切れで解けなかった点を不可能とみなさない。証明できていない場合は「試した候補の中で最小」と表示する。

年間費用へ拡張する場合、季節・曜日区分・特別ダイヤ・気象の出現数に対応する重みを事前に定める。4代表週を52倍するだけでは通年の連続性・請求ピーク・極端気象を再現できない。通年の成立性を主張するには、追加の長期連続評価が必要である。1年の気象を長期の気候平年とみなさない。

### 13.4 政策・運用提言の形

提言は「PVやBESSを増やすべき」で止めず、同じ便を運行できる条件で、追加BESS1単位、受電増強、充電器追加、予備ICE、予報／運用改善の費用対効果を比較する。設備間に補完関係がある場合は単独効果の和で代用しない。

主な出力は、年間化の根拠がある範囲での運用費・設備費、追加投資、モデル内の運行成立率、買電ピーク、BEV営業距離比、燃料使用、CO2削減量と増分費用。CO2は系統電力と燃料の算定範囲・係数・年を明示する。走行時ゼロ排出とライフサイクルゼロを混同しない。

補助金を検討する場合、事業者の負担軽減、行政の支出、社会全体の資源費用を分離する。金額と条件を仮定する政策感度と、現行制度の適用判断を区別し、制度を扱う時点で公的資料を確認する。単一営業所・限定された週の結果を全国一律の最適政策としない。

---

## 14. システム・画面・帳票の拡張要件

### 14.1 Prepare・API・実行管理

期間選択をフロントエンドだけに追加しない。リクエスト検証、Prepare、実効scenario、solver入力、Rollingの状態、保存結果、独立検証、帳票まで同じ期間とハッシュを伝える。

単日入力との後方互換性を保持し、schema migrationを明示する。不明な多日フィールドを無視して1日実行する処理は禁止する。旧出力の96枠が、新7日出力の672枠として読めてしまわないよう型・版を検査する。

ジョブは日付・段階・Rolling時刻・処理中／失敗／受理済みを表示する。中断・再開時に同じstepを二重実行しない。入力変更後の古いcache、異なるPV曲線やfleetのcheckpoint、異なるSHAの結果を再利用しない。

### 14.2 画面の最低要件

期間開始・終了、各日の曜日と適用ダイヤ、時刻表版、日別便数、PV由来日・データ品質、予報種別、親ケース、車両・設備条件、初期／終端方針を実行前に確認できること。欠損や履歴未確認の理由を、実行ボタンの裏で黙殺しない。

結果画面は日表示と週表示を切り替えられ、日付を省略した同じ時刻ラベルの重複を避ける。15分枠を選ぶと、当該枠の車両位置・担当便・SOC・充電器・電力収支を確認できること。

`履歴推定値 / 履歴予報 / 標準曲線proxy / 合成ストレス`、`可行 / 最適性未証明 / 診断用 / 失敗`を明示する。受理したRolling数と予定数を表示し、1stepの失敗を緑色の「完了」で隠さない。

### 14.3 発表で必要な図表

| 図表 | 必ず併記する情報 |
|---|---|
| 7日連続の需給・PV利用先 | 日付、曜日区分、単位、PV原本、抑制、買電、BESS、負荷 |
| BEV・BESSの残量 | 上下限、日跨ぎ、初期・終端、運行と充電、違反数 |
| 車両運用図 | 車両ID、便、回送、給油、充電、未充足の有無 |
| 日別・週別の費用 | 運用・在庫・車両評価・設備費を分離、正本台帳 |
| 四季×天候12曲線 | 使用日数、平均・分位帯、単位、分類規則、除外日数 |
| 計算・予報性能 | 168stepの時間と成功率、forecast lead time、テスト期間 |
| 設備・リスク比較 | 同じ運行要求、成立／不成立／未解決、費用と制約原因 |

日本語中心、白背景、文字を小さくし過ぎない。図の見栄えより、単位・元データ・制約への対応を優先する。将来PPT化する際は話す要点をノートに記載する。

---

## 15. 必須回帰・受入試験一覧

各テストに、対象要件、入力fixture、実行コマンド、期待値、実際の結果、コードSHAを保存する。以下のIDは要件追跡用であり、既存テストの名前を勝手に変更する指示ではない。

| ID | 試験内容 | 合格基準 |
|---|---|---|
| Q01 | 車両別maxSoc=0.90等の適用 | solverと独立検証の両方が上限超過を拒否 |
| Q02 | 初期・終端・不使用BEVの残量 | 全車両の境界・単位が一致、検査の抜けなし |
| Q03 | 旧比較ケースの修正版再実行 | 2ケースとも新SHAで実行、旧診断結果と分離 |
| T01 | 平日・土曜・日曜休日 | 各公式便集合と正規化便集合が一致 |
| T02 | SAT_HOL共通ダイヤ | 日曜の欠落、土日の重複生成がない |
| T03 | 祝日・事業者特別ダイヤ | 日付別例外の優先と出典が一致 |
| T04 | unknown・空service_id | 正式入力が推測WEEKDAYで通らない |
| T05 | 24時以降の便・時刻順 | 元運行日を保持し、翌暦日時刻へ正しく変換 |
| T06 | 短区間便・出入庫便・上下方向 | 時刻だけの誤結合・誤削除なし |
| T07 | 距離・operator_id欠損 | 該当する正式実験を拒否、理由を特定 |
| T08 | 改正日の前後・cache | 適切な版を選択、古いcacheの誤利用なし |
| W01 | 2025暦年・JST境界 | 15分取得なら35,040区間、重複・欠損を報告 |
| W02 | period_endと年境界 | 区間始終端・日付帰属が正しい |
| W03 | W/m²、kWh/m²、kW、kWh、mm/h | 単位変換と時間積分が独立計算に一致 |
| W04 | 再標本化 | 積分量保存、粗いデータの変動を捏造しない |
| W05 | 昼欠測と夜0、極低日射 | 欠損を正常0へ変更せず、悪天候を不正に除外しない |
| W06 | 分類閾値と降水欠測 | 境界値が仕様通り、不明雨量を0としない |
| W07 | 12区分と小標本 | n・使用日付・平均・分位・不十分状態を保存 |
| W08 | 日射→PV変換 | 方位・設備・上限・変換版が固定、単位整合 |
| M01 | 1日と多日N=1の互換性 | 修正済み単日経路と同じ入力・制約・集計 |
| M02 | 2–3日SOC・位置・充電接続 | 日境界に無料エネルギー・車両瞬間移動なし |
| M03 | ICE燃料と給油 | 実際の給油・時間・費用で連続運行成立 |
| M04 | 金→土→日→月・月境界 | 日ごとの便集合とPVが正しく切替 |
| M05 | 7日一括 | 全対象便充足、672区間／673状態境界とtailの整合 |
| M06 | 終端・翌日準備 | 最終在庫の使い逃げと車群合計だけの見せかけなし |
| M07 | ポート・部分時間充電 | 接続数、拘束時間、最大電力、実充電量が一致 |
| M08 | 受電・PV・BESS・燃料収支 | 元物理量から独立再計算して制約違反なし |
| O01 | 7日Rolling | 168実行区間、実行済み672枠、重複・欠落なし |
| O02 | 予報発行時刻 | decision_atより未来の情報が流入しない |
| O05 | 予報外れ時の電力追随 | 共通の宣言済み方針で需給を処理し、不足を隠さない |
| O03 | 確定済み便と位置 | 過去・進行中・拘束中の判断を変更しない |
| O04 | 再開と入力変更 | 中断再開と連続実行が整合、異なるhashを拒否 |
| U01 | 学習・検証・テスト分離 | テスト週が代表曲線・誤差学習に混入しない |
| U02 | 系列相関・低日射連続 | 独立乱数だけでなく実日／ブロック性を保持 |
| U03 | 非予見性 | 同じ情報で先頭実行判断がシナリオごとに変わらない |
| U04 | 運用失敗の集計 | 失敗週を除外せず、原因・母数を保存 |
| C01 | 車両×日の課金 | 重複課金・週1回への誤集約なし |
| C02 | 日・週・Rollingの台帳 | 実行区間だけを合計、帳票間差1e-6円以内 |
| C03 | 在庫・給油・終端費用 | 二重計上なし、期間中の調整が正しく相殺 |
| C04 | 月額料金・設備費 | 仮定と実費を分離、固定費の二重計上なし |
| E01 | provenanceと比較条件 | 日付・入力・fleet・曲線・料金・SHAを完全追跡 |
| E02 | 最適性・失敗理由 | 時間切れと不可能、Stage別gapと全体評価を分離 |
| E03 | ローカル回帰・統合試験 | 関連試験と全体suiteを実行、失敗・未実行も記録 |

物理量の数値許容差は単位ごとに明示し、solverの設定と独立検証の両方を記録する。数%のSOC超過を数値誤差として許す設定は禁止。会計の帳票間差と、solverの目的値の近似誤差は別の問題として扱う。

---

## 16. 作業順、成果物、完了判定

| Work package | 実施内容 | 主な成果物 | 次段階へ進む条件 |
|---|---|---|---|
| WP0 | 到達可能経路の調査・SOC等の是正 | 現状監査、再現fixture、修正と試験、修正版2ケース | 物理契約の抜けを閉じる |
| WP1 | 公式路線・時刻表と土休日の監査 | 路線台帳、公式証跡、日別calendar、バグ報告と回帰 | 対象期間の便集合が確定 |
| WP2 | Solcast年データ・品質・12曲線 | 原本、正規化系列、品質台帳、分類・12区分、図 | 権限・時刻・単位・データ範囲が確定 |
| WP3 | 多日モデル・UI/API・会計 | 1日回帰、2–3日、7日一括／Rollingの検証 | 日跨ぎと全物理・会計・来歴契約が成立 |
| WP4 | 予報／proxyと不確実性 | 分割・予報台帳、外部テスト、提案運用の比較 | 漏洩なし、実行可能な判断、失敗も評価 |
| WP5 | 四季・設備・費用の比較 | 実験プロトコル、指標、図表、限定付き結論 | 全主張が受入済み実験へ追跡可能 |

WP1・WP2の監査はWP0と並行してよい。WP3のデータ型や試験の設計も先行できるが、未修正モデルで正式な7日結論を出さない。各段階を小さな論理単位のcommitに分ける。新データの取得や未知の業務ルールが必要な箇所だけを停止し、独立して進められる監査・試験は進める。

### 発表ごとの到達点

学内中間発表の最小到達点は、修正版の物理契約、土休日を含む7日連続の決定論的運用、四季の代表週比較、実データ由来の12曲線と標本数、失敗／限界の開示である。不確実性対応が途中なら、検証済みの系列生成と未実施範囲を明示する。

電気学会に向けた重点は、単なる四季の図の並列ではなく、日次／週次中立、先読み期間、予報情報、連続低日射のどれが運用費・成立性を変えたかを分離すること。予報不確実性の価値を主張する場合はWP4の外部テストまで必須とする。

修士論文の拡張は、複数週・長期連続評価、設備・受電・予備車の比較、設備費を含む条件付きの政策示唆とする。提出日程は本書で推測せず、実際の締切と計算資源を確認して割り当てる。

---

## 17. 納品物の論理一覧と記録規則

既存の保管規約に合わせて配置する。以下は成果物の役割の一覧であり、同名ファイルの重複生成を要求しない。

| 成果物 | 最低限の内容 |
|---|---|
| 現状監査・要件追跡表 | 確認済み／推論／未確認、実行経路、要件IDと実装箇所 |
| 路線・便監査台帳 | 原ID対応、日付別便集合、時刻表版、差分と出典 |
| 公式確認証跡 | URL・取得日時・画面・選択条件・原テキスト・hash |
| 生データ問題台帳 | 再現、原因、修正、影響範囲、残る不明点 |
| Solcast取得manifest | 地点、期間、項目、権限、分割取得、原本hash |
| 正規化日射／PV系列 | timezone-aware区間、値、単位、品質、変換版 |
| 全年記述用12曲線 | 集計対象全日、平均／分位／標本数、除外理由 |
| 学習分割別12曲線 | 学習日付、test除外、分類／モデル版 |
| 予報と実現値の台帳 | issue time、valid time、lead time、source kind |
| 多日prepared manifest | 親ケース、全日calendar、fleet、初期・終端、料金・設備 |
| 実行状態とcheckpoint | 各stepの入力／実行hash、引継ぎ、確定済み判断 |
| 日・週の費用正本 | 実行物理量、在庫差、使用日、費目、独立検算 |
| 実験結果・失敗一覧 | 全実行、未充足、制約違反、gap、時間、停止理由 |
| 発表用図表データ | 表示値と元精度、元台帳の参照、図の意味と限界 |

日付・設定を変えるたびに原本を上書きしない。機密・ライセンス制限のある時系列は公開Gitへ置かず、公開可能なメタデータ・取得スクリプト・検査結果と分離する。論文PDFや第三者時刻表を権限確認なしで再配布しない。

`DEVELOPMENT_NOTES.md` へ、作業日時JST、変更理由、変更したファイル／関数、数式・単位・制約の変更、試験コマンドと結果、データ・結果の失効範囲、未解決事項を追記する。別々の断片メモだけで経緯が追えなくなる状態にしない。

最終報告は「変更したこと」「試験で確認したこと」「確認できていないこと」「正式研究に使える結果」「使えない結果と理由」を分ける。完了の言い換えで、未取得データ・未実行の統合試験・未確認の時刻表を隠さない。

---

## 18. 特に禁止する誤実装・誤解釈

| 禁止事項 | なぜ不可か |
|---|---|
| 日ごとにBEV/BESS/Fuelを初期値へ戻す | 無料の補給で長期成立性を偽る |
| RAINを日曜ダイヤの検証済みケースとみなす | 既存ケースは同一平日のPV反実仮想 |
| 全日264便、BEV35/ICE25をコードへ固定する | 日別ダイヤ・実効fleetが変わる |
| 現在の時刻表を2025の正本へ無言で上書き | 正当な改正と原データバグを混同する |
| unknownを平日、欠損PVを0と推測する | 不完全データを検証済みに変える |
| 低PV三分位を気象としての雨と断定する | 降雨・季節・日長を区別できない |
| 12枚の曲線を描くため値を生成する | 年間実データに基づく成果ではない |
| 標準偏差をそのまま予報誤差と呼ぶ | 日間変動と予測残差は異なる |
| 実現した天候を事前に知る予報評価 | 将来情報の漏洩になる |
| 全年平均作成にテスト週を含め、その週で精度を主張 | 学習と検証が独立していない |
| シナリオごとに先頭実行を別々に選ぶ | 実行時に不可能な先見的判断になる |
| 週末の残量を使い切り費用だけ比較する | 初期在庫の費用を過少評価する |
| Rollingの全lookahead費用を足す | 未実行部分を何重にも数える |
| SOC上限超過を後処理clipで隠す | 解・エネルギー・費用の整合を破壊する |
| 時間切れで解がない点を不可能と呼ぶ | 解法の限界と物理限界は別 |
| 既存評価額をそのままTCOと呼ぶ | 設備費・固定費・実支出の意味が違う |
| 晴雨で必ず違う配車になるよう細工する | 研究結果を事前に強制する |
| 7日成功から年間運行・一般政策の保証へ飛躍する | 評価範囲と外的妥当性を超える |

---

## 19. 正式実験前に確認する最小限の未確定事項

次の事項は、調査で解決できるものを先に解決した上で、一括して確認する。すでに原データに存在する情報をユーザーに再入力させない。

| 決定事項 | 本書の基準案 | 確認が必要な理由 |
|---|---|---|
| 2025年の範囲 | 暦年2025 | 年度2025とは3か月ずれる |
| 2025時刻表の履歴欠落時 | 歴史再現は保留、別途反実仮想を提案 | 異なるデータを実運行再現と呼べない |
| BESS終端方針 | 日次中立を保存し週次中立を比較 | 既存指導の変更になる |
| BEV終端・8日目 | 車両別の初期残量以上＋次便成立を基準案 | 比較費用と長期成立性を左右する |
| 日天候の定義 | 日中降水＋clear-sky比の研究用分類 | 公式の日天気概況とは別 |
| Solcastの権限 | 既存権限／CSVで進め、追加購入しない | 取得範囲・利用料金・配布条件 |
| 制限時間・gap・計算資源 | 段階試験から提案し正式実験前に固定 | 実行時間と主張精度の両立 |
| 予報の主張範囲 | 履歴予報がなければproxyと明記 | 実際の予報誤差とは区別が必要 |

これらの未確定事項があることを理由に全作業を止めない。路線IDの抽出、原データ監査、SOC再現、契約設計、テスト整備など、依存しない作業を先行する。

---

## 20. 参照資料と既存実装の入口

以下は要件作成時に確認した資料、またはコード上の調査入口である。文書の新旧を確認し、最新監査と原入力を優先する。特に8月の旧合格記録と9月のSOC監査が矛盾する場合、旧合格だけを引用しない。

### リポジトリ

- [R1] 最新READMEの2026-09-07/08追記。`https://github.com/CYLIU2003/master-course/blob/5fac09d9a5775192ab8d3e507c5e72a6fd36afe2/README.md`
- [R2] 凍結2ケースの実効パラメータ。`https://github.com/CYLIU2003/master-course/blob/5fac09d9a5775192ab8d3e507c5e72a6fd36afe2/docs/thesis/weather_results_bb0c005/experiment_parameters.md`
- [R3] 占部先生への説明とSOC追加監査。`https://github.com/CYLIU2003/master-course/blob/5fac09d9a5775192ab8d3e507c5e72a6fd36afe2/outcome/2026-09-07_urabe_progress_review/README.md`
- [R4] 既存日天候分類。`https://github.com/CYLIU2003/master-course/blob/5fac09d9a5775192ab8d3e507c5e72a6fd36afe2/src/preprocess/weather/solcast_typical/classify.py`
- [R5] service_id正規化。`https://github.com/CYLIU2003/master-course/blob/5fac09d9a5775192ab8d3e507c5e72a6fd36afe2/bff/services/service_ids.py`
- [R6] 作業・実験・会計契約。`https://github.com/CYLIU2003/master-course/blob/5fac09d9a5775192ab8d3e507c5e72a6fd36afe2/AGENTS.md`

既存実装の調査入口は `src/runtime_scope.py`、`src/tokyu_bus_data.py`、`src/tokyu_shard_loader.py`、`src/research_dataset_loader.py`、`scripts/catalog/build_tokyu_bus_data.py`、`src/optimization/common/solcast_pv_profiles.py`、`src/preprocess/weather/solcast_typical/{loader,classify,aggregate,forecast}.py`、`scripts/weather/build_solcast_typical_curves.py`、`scripts/sync_solcast_registry.py`、`src/result_exporter.py`、`scripts/verify_frontend_run_artifacts.py`。入口の列挙は全関数を再検証したという意味ではない。現在の呼出し元を追う。

### 東急バス公式

- [T1] 2026-09-01の弦巻・淡島ダイヤ改正。`https://www.tokyubus.co.jp/news/003835.html`
- [T2] 2025-07-01の改正。`https://www.tokyubus.co.jp/news/003546.html`
- [T3] 公式時刻表検索への入口。`https://www.tokyubus.co.jp/`、`https://transfer.navitime.biz/tokyubus/pc/map/Top?window=diagram`
- 路線図入口。`https://www.tokyubus.co.jp/route/routemap/`
- 2025年告知一覧。`https://www.tokyubus.co.jp/news/2025.html`

### Solcast・ブラウザー公式資料

- [S1] Historical Time Seriesの位置づけ。`https://www.solcast.com/time-series`
- [S2] Irradiance and Weather API。`https://docs.solcast.com.au/docs/section/irradiance-weather-data`
- [S3] 大学研究向け窓口。`https://www.solcast.com/data-for-researchers`
- [S4] 出力項目・単位。`https://docs.solcast.com.au/docs/output-parameters`
- 認証・応答形式・OpenAPIの入口。`https://docs.solcast.com.au/`
- [A1] agent-browser導入。`https://agent-browser.dev/installation`
- [A2] snapshotと操作参照。`https://agent-browser.dev/snapshots`

### 研究比較の出発点

- [L1] Bie, Qin, Wu (2024), *Optimal electric bus scheduling method under hybrid energy supply mode of photovoltaic-energy storage system-power grid*, Applied Energy 372, 123774. DOI `10.1016/j.apenergy.2024.123774`。PV・蓄電・系統の配車／充電と天候比較を既に扱う。日次の定式化・仮定を確認し、日間状態の比較対象として読む。
- [L2] Zhong et al. (2024), *Joint optimization of electric bus charging and energy storage system scheduling*。Project原本 `No16(1).pdf`。バス充電と蓄電池、料金・劣化・PV変動への対応とMonte Carlo評価を扱う。単なるBESS統合や乱数評価を新規性としない。
- [L3] Vendé, Desaulniers, Kergosien, Mendoza (2023), *Matheuristics for a multi-day electric bus assignment and overnight recharge scheduling problem*, Transportation Research Part C 156, 104360. DOI `10.1016/j.trc.2023.104360`。出版社の抄録・導入を確認。複数日のブロック割当と夜間充電、日次方式との比較を扱う。「多日化そのもの」や「翌日の残量を考えるだけ」を新規性としない。既定の車両ブロックと同種BEVを前提とする同論文に対し、本研究で扱う混在車両・昼間PV・BESS・気象系列・更新情報の差を明確にする。
- [L4] Fantin et al. (2026), *Electric bus rapid transit supported by solar generation: A scalable economic model for zero-emission urban transit*, Energy Policy 209, 114981. DOI `10.1016/j.enpol.2025.114981`。太陽光を含むTCO・政策志向の経済評価がある。独自性は「政策を論じること」ではなく、その結論へ至る運行と不確実性の検証に置く。

この4本だけで文献調査を完了としない。研究目的、固定／変動するダイヤ、混在車両、時間範囲、日跨ぎ状態、PVの出典、予報の発行時刻、非予見性、料金・設備費、検証方法を同じ列で比較する。数値効果や優劣を引用する際は本文・条件を再読する。

---

## 21. 最終完了条件

**完成と判定できるのは、日付に対応した入力があり、同一車両の状態をつないだ連続運用が成立し、その実行から費用が再計算でき、季節・予報・設備の何が差を生んだかを説明できる場合である。**

取得できていないSolcastデータ、未検証の2025時刻表、空の天候区分、未受理のRolling、SOC問題、解法の探索不足が残る場合は、その範囲を明記する。「資料ができた」「グラフが出た」「solverが終了した」を研究完成の代わりにしない。
