{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "features/vehicle_instance.schema.json",
  "title": "VehicleInstance",
  "type": "object",
  "required": ["vehicle_id", "vehicle_type_id"],
  "properties": {
    "vehicle_id": { "type": "string" },
    "vehicle_type_id": { "type": "string" },
    "depot_id": { "type": "string" },
    "initial_soc": { "type": "number", "minimum": 0, "maximum": 1 }
  }
}
