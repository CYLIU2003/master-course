# 実装エージェント指示書：７日連続・４季節・天候不確実性

## 目的

既存の弦巻営業所の２条件を親として、対象路線を保持し、各日の正しいダイヤとPVを用いた連続７日間運用を実現する。SOC・位置・充電設備・会計を日跨ぎで整合させ、４季節比較と不確実性評価へ接続する。

この文書は実装計画である。記述される新しい概念名を、既存APIや関数名だと仮定してはいけない。まず実コードを確認し、既存の入口と構造を再利用する。

## 開始時の原則

対象は `CYLIU2003/master-course`。今回の読取り基準は `5fac09d9a5775192ab8d3e507c5e72a6fd36afe2`。作業開始時のHEADとdirty状態を記録し、既存作業を破棄しない。AGENTS.mdと該当する既存指示を読む。mainへ未検証変更を直接投入しない。GitHub Actionsの有料実行を勝手に開始しない。

UI/BFF → Prepare → canonical problem → Stage 1 → Stage 2 → rolling → final accountingという到達可能な経路を追う。未使用の古い関数を直して完了扱いにしない。旧結果を新しいコードの結果と呼ばない。

## チケット0：凍結入力の棚卸し

親２シナリオのID、路線レコードと枝・方向、事業者、ダイヤ版、実車ID、充電器、PV/BESS、料金、初期状態、preparedハッシュを一覧化する。既存は2025-08-05を共通運行日とし、PVだけ別日由来の比較である。低PV条件を日曜の実ダイヤと見なさない。

対象路線を推測して減らしたり、営業所全路線へ勝手に増やしたりしない。公式系統名数と内部路線レコード数を区別する。取得できない原本は未取得と記録し、代用品で研究用Prepareを成功させない。

成果物：親入力一覧、経路図の文章説明、原本・派生データのハッシュ台帳、未確認事項一覧。

## チケット1：SOCと暦のP0修正

SOCの指定上限が全経路で反映されるよう修正する。Stage 2が100％容量で代用していた既知問題を確認し、車両ごとのmin/maxを用いる。rolling中の初期条件・終端・独立検証も同じ値を参照する。最終出力のclipは禁止。90％上限で93％を不合格にする試験、車両別上限、境界一致を入れる。

暦については添付差分をレビューし、現コードと一致する場合のみ作業ブランチへ適用する。`src/optimization/common/service_calendar.py` に加え、`src/tokyu_shard_loader.py`、BFF等の不明ID→weekdayの既定値を調べる。研究用は不明IDで停止する。既知aliasは統一し、SAT/SUN_HOL/SAT_HOLを混同しない。行の複数フィールドに矛盾がある場合は停止する。

平日固定PV反実仮想は明示した別モードにだけ残す。実日付７日ケースへ例外を流用しない。添付の33件は局所検証にすぎないため、元リポジトリで関連試験・全体回帰試験を実行し、元からある失敗と新規失敗を分けて記録する。

## チケット2：agent-browserによる公式原本照合

指定ツールの公式手順と導入状態を確認する。未導入なら許可された環境で導入する。今回の配布環境ではnpmへの名前解決に失敗したため未実施である。ツールが使えない場合は他の取得結果を指定ツール実施済みと書かない。

東急バス公式入口から対象路線・枝・方向をたどり、平日／土曜／日祝日を確認する。UIの状態や参照をその都度読み、未知の要素IDやURLパラメータを捏造しない。表示基準日、改正日、各便の行先、短区間便、全停留所時刻を確認する。低頻度の確認とキャッシュを優先し、規約・アクセス制限・CAPTCHAを回避しない。

2026-09-01改正には途中停留所だけの変更がある。終点・始発の比較だけで合格にしない。2025年の保存ダイヤがなければ、2026年のデータを2025年実績として上書きしない。特別運行日・祝日・年末年始・催事の適用範囲を確認する。

成果物：原本保存、取得日時・表示日・版・URL、全対象便の照合CSV、重複・欠損・異常の原因と修正差分。公開情報に到着時刻や回送距離がなければ、明示した推定根拠か別原本が必要である。

## チケット3：年間日射量とラベル

Solcastの公式Historical API、契約権限・解像度・残枠・料金を確認する。最初は１か月で実応答の検証を行い、その後暦年2025の12か月を取得する。年度も必要なら2026-03まで追加し、原本は共通、集計はCY/FY別にする。APIキーは環境変数。取得枠・課金の不明な要求を勝手に続けない。

添付 `tools/solcast_year.py` は公式仕様に基づく未認証試作である。実通信の成功を前提にせず、スキーマ・日境界・分解能を確認する。原本変更、欠測0埋め、時刻の恣意的調整は禁止。

`data/external/solcast_raw/` から `data/derived/pv_profiles/` および実際のシナリオ入力まで既存変換を追う。`scripts/sync_solcast_registry.py` が最初のCSVだけを拾う方式を複数原本の台帳へ拡張する。元の720枠の８月データには31日の日中がない点を回帰ケースにする。

天候分類規則を出典付きで固定し、混合天候や夜だけの雨を確認する。既存 `solcast_typical/classify.py` の発電量三分位をそのまま晴・曇・雨へ流用しない。年間原本から12区分を集計する。標本不足は不足と示し、見栄えのための架空曲線は禁止。

## チケット4：日付ごとの準備処理

`src/optimization/common/builder.py` の初日複製を、実日付モードでは日別入力へ置き換える。日別便を構成する既存Prepare/ETLを確認し、同じ公開入口へ接続する。新規スクリプトだけで本体改修の代わりにしない。

運行日、実時刻、適用カレンダー、ダイヤ版、PV・料金の参照日を別々に持つ。tripの複合キーは日付を含め、元trip_idを追跡可能にする。24時以降、金土・土日・祝日・月跨ぎを検証する。日射原本のinterval endと開始時刻を混同しない。

準備画面に各日の便数・PV行数・出典版と欠損を表示する。キャッシュは全日列・入力原本・車両集合・モデル版で識別する。どれか１日が不足すれば研究用の開始を止める。

## チケット5：連続状態とrolling台帳

EV/BESSの全区間のSOC上下限・充放電・消費をつなぐ。日初SOCへ戻す処理を廃し、実際の終端を引き継ぐ。車両位置、帰庫・回送・翌日の出庫可能性、充電器の占有もつなぐ。補機・回送エネルギーの二重計上を防ぐ。

週の評価区間と前後の先読み・回復区間を定義する。週末に電池を使い切って運行費だけ安く見せない。週末同残量を基準案にし、終端条件の感度と翌朝出庫を確認する。日ごとの同残量制約を無意識に残して、日跨ぎ蓄電の価値を消してはいけない。

毎時の先頭実行区間だけを採用して接続する。重複・飛び・古い状態からの再計算・途中失敗後の偽の継続を防ぐ。１日版の最終会計を踏まえ、週の採用区間に基づく唯一の会計台帳を設計する。１回のrollingごとの予測期間全体を合計しない。

## チケット6：費用・研究評価

kW、kWh、L、km、円の測定位置・単位を明記。車両使用日費用と保有・設備投資費を分ける。ピーク料金は実契約の請求期間・需要測定間隔と期間前の状態を扱う。必要な実単価が未確定なら仮定と明記し、実請求見積りと呼ばない。

評価週は最適化結果を見る前に、季節・データ完全性・曜日構成・気象分布で選ぶ。季節別通常週と連続低日射の厳しい週を区別する。PVだけの対照実験、気温まで変える季節実験、手法間比較を混ぜない。

2025年全体の記述用代表曲線と、評価データを除いたモデル用曲線を別保存する。過去予報の発表時刻と実績の組がない限り、予報誤差分布を検証済みと書かない。完全予見は上限参照、不確実性の人工ケースは人工と明記。日跨ぎ相関を残す。

## 最低限の統合試験

１日で旧仕様を再現する試験、金→土の便数変化、土→日の切替、平日の祝日、月末・年末、24:30の便、翌朝充電、同じ車両IDの連続SOC、異なる車両上限、途中rolling失敗、PV欠測、キャッシュ汚染、会計重複、最終日SOCの使い逃げを検査する。

小規模の２日・７日問題は参照MILPと照合する。本規模は全便充足、全物理制約、独立会計、計算時間・解の品質を記録する。Stage 2が最適でも２段階全体の大域最適と主張しない。探索ネットワークの枝刈りやfallbackを隠さない。

## 完了時に必要な証拠

変更ファイルと数式・単位への影響、テストコマンドと実出力、元入力・新入力の差分、凍結SHA、データ原本のハッシュ、各日便数とPV出典、EV/BESSの連続状態、全rolling採用区間、最終会計、残る未解決事項を提出する。

変更ごとに `DEVELOPMENT_NOTES.md` へ実日時、目的、変更箇所、検証、数理・比較への影響、未解決点を追記する。挙動が変わればREADMEと現在のblocker文書も更新する。既存の記録を消さない。

未達があれば診断用と明示し、研究採用可能と表示しない。最後の判定は「コードが動いたか」ではなく、「指定した７日を、指定した便と設備で、翌日も走れる状態を保ち、追跡可能な費用で運行できたか」で行う。
