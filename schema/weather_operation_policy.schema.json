{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "weather_operation_policy.schema.json",
  "title": "WeatherOperationProfile",
  "type": "object",
  "required": [
    "operation_mode",
    "initial_soc_mode",
    "pv_marginal_charge_cost_yen_per_kwh"
  ],
  "properties": {
    "operation_mode": { "enum": ["aggressive", "normal", "conservative"] },
    "final_soc_floor_percent": { "type": ["number", "null"], "minimum": 0, "maximum": 100 },
    "final_soc_target_percent": { "type": ["number", "null"], "minimum": 0, "maximum": 100 },
    "final_soc_target_tolerance_percent": { "type": ["number", "null"], "minimum": 0, "maximum": 100 },
    "initial_soc_mode": { "enum": ["unchanged"] },
    "initial_soc_min_percent": { "type": ["number", "null"], "minimum": 0, "maximum": 100 },
    "initial_soc_max_percent": { "type": ["number", "null"], "minimum": 0, "maximum": 100 },
    "midday_charge_priority": { "type": ["number", "null"] },
    "bev_duty_bias": { "type": ["number", "null"] },
    "ice_backup_bias": { "type": ["number", "null"] },
    "grid_risk_penalty_multiplier": { "type": ["number", "null"] },
    "pv_marginal_charge_cost_yen_per_kwh": { "const": 0.0 }
  },
  "additionalProperties": false
}
