# 先行文献と現行モデルの求解時間比較（2026-08-14）

## 2026-08-15 追補：純粋集約networkの264便実測

clean commit `94ce217`から同一の低PV・BEV消費1.2倍・900秒条件をfresh Prepareし、
`output/2026-08-15/run_20260815_1155`で実測した。純粋集約networkは初期変数を
848,980から536,180、制約を286,282から233,579へ減らし、pre-optimizeを
165.812070秒から130.419562秒へ短縮した。一方、Gurobi時間は474.744153秒から
505.784332秒へ増え、incumbent 61,883.346234円、認証下界57,986.661708円、
認証gap 6.296823%、node 1は変化しなかった。

したがって、「モデル構築の無駄な車両ラベル拡張」は解消したが、「数百秒で1%
認証」は達成していない。No06やNo63の数百秒結果へ近づける次の候補は、root下界を
強化する厳密分解、または厳密MILPとは別claimで管理するALNS・ラグランジュ分解で
ある。同じ定式化を時間だけ延ばして再実行することは次の優先作業にしない。

## 2026-08-15 追補：文献比較を受けた純粋集約ネットワーク

`f1690c6`の負の実験で残った車両ラベル付き連続flowを、認証済み同一ICE群から
完全に除去した。配車被覆はgroup assignmentを直接参照し、便間接続・fragment
開始終了・営業所resetは三層の整数networkだけで表す。燃料、回送、CO2、車両
固定費、vehicle-day費は同一性証明済みの代表係数をこのnetworkへ付与し、解後は
整数pathをcanonical車両IDのprefixへ分解する。さらに、集約networkの方が小さく
なることをauditで証明できる群だけへ適用し、小規模で逆に変数が増える群は元の
離散flowに残す。

1便・同一ICE 2台の厳密比較では、離散モデル398変数・58二値に対し、純粋集約は
300変数・55二値となり、目的値、使用台数、担当便、回復車両IDが一致した。
2-fragmentとPhase-3 warm startを含む回帰、および全1491テストも通過した。

この後の264便実測は直上に記録した。モデルは縮小したが認証gapは改善しておらず、
「数百秒で1%認証できるようになった」とは主張しない。

## 2026-08-15 追補：PDF表の再確認と同一条件の集約定式化実測

`先行文献/`の25本を再確認し、特にNo06、No16、No61、No63、No64の計算時間表を
PDF本文とページ画像で照合した。数十秒から数百秒の報告は実在するが、主に
次の三種類である。

1. 車両の担当便・運行を所与とした充電、PV、ESSの最適化（No16、No61、No63、No27）。
2. ラグランジュ緩和、動的計画、ALNS-SA等の分解・近似法（No63、No06）。
3. 現行モデルより離散変数が少ない問題または大規模計算機による計算（No64）。

最も近いNo06の統合問題では、Gurobiは20便1.78秒、50便617.6秒だが、200便と
418便では6時間以内に可行解を得ていない。418便202.3秒はALNS-SAであり、
Gurobiとの差0.7%は50便ケースについてだけの比較である。したがって
「先行研究は数百秒」という観察は正しい一方、それを264便の完全後続・個別
車両SOC・充電器・PV・BESS統合MILPの1%厳密認証時間とみなすことはできない。

この比較を受け、25台の同一ICEを三層の集約fragment networkへ置換した定式化を
clean SHA `f1690c6a9a6145086a96df05193794065e6c2f40`で実測した。対照は同じ入力・
900秒共有予算の`10a6621`である。

| 指標 | `10a6621` labelled binary flow | `f1690c6` layered aggregate + continuous labels |
|---|---:|---:|
| 初期全変数 | 780,113 | 848,980 |
| 初期二値変数 | 739,728 | 507,194 |
| 初期制約 | 355,581 | 286,282 |
| pre-optimization | 166.509116秒 | 165.812070秒 |
| cost-stage solve | 474.988037秒 | 474.744153秒 |
| incumbent | 61,883.346234円 | 61,883.346234円 |
| certified bound | 57,986.661708円 | 57,986.661708円 |
| certified gap | 6.296823% | 6.296823% |
| explored nodes | 1 | 1 |

二値変数は232,534個減り、制約も69,299本減ったが、車両ラベル付き連続拡張を
残したため全変数は68,867個増え、求解結果と時間は実質的に変わらなかった。
これは「二値数だけを減らせば数百秒で1%認証できる」という仮説に対する負の
実験結果である。次の厳密実装は連続ラベル拡張自体を除く便鎖列／duty masterと
認証可能なrecourseを対象にする。No06やNo63型のALNS・ラグランジュ分解を導入
する場合は、厳密MILPとは別モード・別claimとして比較する。

## 2026-08-15 追補：文献比較から発見したseed探索予算の不整合

文献の数百秒級結果と比較する際、現行Phase 4では本体MILPより前のseed品質も
確認した。frontendの正式設定は固定-duty近傍を最大64候補に制限するが、従来は
direct/pairwise探索とmatching検証が全枠を消費し、enabledである
suffix exchange、powertrain duty swap、identity exchangeが0回のまま終了し得た。
さらにroute-band再分割は45秒の別wall budgetを表示しながら、共通64候補上限に
達すると一度も開始されなかった。

これを、局所近傍用の明示的reserve、改善解からのwhole-duty activation restart、
route-band専用の有限候補枠に修正した。保持済み`8066330`低PV入力の診断再構成では、
元の13 BEV/19 ICE seed 707,518.152327円を完全再現した後、32.178553秒で
15 BEV/17 ICE、697,433.686483円の候補を得た。独立下界694,498.136390円に対する
認証gapは0.420907%である。

この値は、過去入力を用いたseed候補生成の診断であり、現在SHAの正式runではない。
また、文献の厳密MILPが数百秒で認証されたことを意味しない。次の比較では
`first feasible incumbent time`、`best incumbent time`、`declared gap certification
time`、`frontend submit-to-terminal time`を別々に記録し、clean commitからのfresh
Prepareと正式実行でのみ性能改善を判定する。

## 2026-08-14 600秒診断で判明した予算二重計上

clean SHA `102546170dc8a07fa91e0b71beaa8c71ca1ea327`からfresh Prepareし、
通常のfrontend/BFF経路で高PVケースを600秒に設定したところ、Phase 3 seedは
607.319707秒、Phase 4 integrated solveは601.350376秒を別々に消費した。
seedのソルバ記録自体は61.586327秒であり、主な超過は60台の動力構成候補ごとの
モデル再構築と未使用BEV近傍探索だった。したがって従来の600秒表示は、文献の
computational timeと比較できる共通の実時間上限ではなかった。

修正後は、strict precheck、同一問題から作る物理的seed 1件、統合モデル構築、
fixed-dispatch recourse、branch-and-boundを一つのwall-clock budgetへ含める。
Phase 4本体が全動力構成を探索するため、seed側の全構成探索は正式なPhase 4の
最適性証明には不要であり、Phase 3感度実験として分離する。この変更後の
最初の264便実測はHTTP終端628.656745秒、最適化604.204202秒となり、旧来の
SeedとPhase 4の二重600秒は解消した。ただしStage 1がモデル構築後にも80秒を
消費してStage 2の時間を失い、Phase 4は初期解なしで可行解を得られなかった。
このrunは求解時間短縮の証拠ではなく、fail-closed診断である。Stage 1/2の
残時間按分を追加修正した後の再計測が必要である。

同じ診断では、単一fragmentを仮定したICE clone集約も実入力へ適用されなかった。
実効条件は1台3fragment、start/end上限100で、変数数は780,112のまま、
600秒時点のcertified gapは1.583730%だった。単一fragment監査で見積もった
290,448 binary削減は仮想的上限であり、現行ケースの性能証拠から除外する。

## 結論

先行文献には数十秒から数百秒で終了する例が多い。ただし、その時間を
現行の Phase 4 統合 MILP とそのまま比較することはできない。短時間例の
多くは、車両ごとの担当便を所与とした充電・PV・ESS 配分、分解法、または
近似ヒューリスティクスである。一方、個別便の車両割当まで含む文献では、
数百秒の結果は近似解であり、商用ソルバが数時間で大規模ケースの可行解を
得られない例も確認できる。

現行モデルが長時間を要する直接の理由は、264便、60台、混成動力、個別
SOC、充電器、PV、BESSを同時に扱うことに加え、正式計算で全可行後続辺を
車両別に展開しているためである。2026-08-13 の比較結果では 678,600 本の
後続辺、780,112 変数、1,598,973 制約、726,240 個の離散 start 値を持つ。
これは、文献 No16 の 19,452 変数や、No64 の最大 98,784 変数より大幅に
大きい。

したがって、時間上限だけを短くするのではなく、次の二つを分けて進める。

1. 正式研究計算は完全後続ネットワークと gap 証拠を維持する。
2. 数百秒級を目標に、便鎖列生成・ラグランジュ分解・集約フローで離散変数
   を減らす。ただし近似法は `NEAR_OPTIMAL` と表示し、正式な最適性認証と
   混同しない。

## 文献別の確認結果

| 文献 | 規模・範囲 | 報告時間 | 比較上の注意 |
|---|---|---:|---|
| [No16: Zhong et al. (2024)](../../先行文献/No16.pdf) | 49台、275便。担当便と充電可能時間は所与で、充電・ESSを最適化。9,946連続＋9,506二値変数 | 線形化MILP 1.5秒、非線形ソルバ 466.28秒（PDF 3, 12頁） | 現行モデルと便数は近いが、車両割当を同時に選ばない。`mip_gap=0` はこの固定運行の充電問題に対する値 |
| [No61: Xiao et al. (2026)](../../先行文献/No61.pdf) | 78台、6路線、508 service loops。所与の運行時刻に対して充電・PV・ESS・系統を最適化 | baseline 34秒、冬89秒、夏147秒（PDF 9–11頁） | 車両・便割当問題ではない。該当箇所に終了gapの記載がなく、最適性証明時間とは比較できない |
| [No63: Hu et al. (2025)](../../先行文献/No63.pdf) | 128台。車両運用は所与。充電とPV-storage-charging stationを最適化 | Gurobi 44.5–1058.9秒、分解法 7.5–93.4秒（PDF 6, 16–17頁） | 数十秒はラグランジュ緩和＋動的計画。Gurobi比の費用差は最大2.7%、多くは1%未満。論文自身が車両運用との協調を今後の課題としている |
| [No64: Manzolli et al. (2025)](../../先行文献/No64.pdf) | 10台、10路線、420往復、10充電器。充電・V2G・劣化・不確実性を扱う | 決定論 52.41–363.05秒、ロバスト 63.00–3002.07秒（PDF 7–8, 16頁） | 7200秒または0.5% gap。80 Xeon cores、314GB RAM。最大98,784変数で、現行モデルの約8分の1 |
| [No06: Zhou et al. (2025)](../../先行文献/No06.pdf) | 5路線、418便。便割当と充放電を統合 | Gurobiは20便1.78秒、50便617.6秒。200便・418便は6時間で可行解なし。ALNS-SAは418便202.3秒（PDF 13–14頁） | 現行モデルに最も近い比較。202秒は近似法であり、大規模ケースの厳密解ではない。Gurobiとの差0.7%は50便ケースでのみ確認 |
| [No43: Wang et al. (2024)](../../先行文献/No43.pdf) | 85便、異種BEV、充電器配置とfleet構成 | 365秒以内（PDF 9頁） | hybrid ITS-FFA、100反復の `near-optimal` 解。厳密gapではない |
| [No55: He et al. (2023)](../../先行文献/No55.pdf) | 70便。充電設備、車両割当、充電を統合した非線形計画 | 約5時間（PDF 15–16頁） | GAを使用し、各反復120個体をNREL Eagleの120 processorsで並列評価。短時間例ではない |
| [No03 (2025)](../../先行文献/No03.pdf) | 32台、充電・交換・再エネ・配電網 | 約30分（PDF 13–14頁） | 配電網規模と台数増加で計算時間が2–3倍ずつ増加 |
| [No27 (2025)](../../先行文献/No27.pdf) | 最大50台の感度。各車両の便予定は所与で、充放電・V2Gを最適化 | 平均24.3–42.6秒、一部227.4秒（PDF 12, 15頁） | 600秒上限、0.1% gap。車両割当は将来課題と明記 |

## 現行結果との同条件内比較

対象は frozen SHA `f46f1e821e6773f7f647dd130b28427bbb3df10d` の
`formal_pair_20260813_route_band_feedback_budget_attested_v7...r7` である。

| 項目 | 高PV | 低PV |
|---|---:|---:|
| 求解時間 | 3,600.80秒 | 18.36秒 |
| feasible incumbent | あり（初期時点） | あり |
| 生Gurobi gap | 1.574% | 8.351% |
| 独立下界を含む認証gap | 1.574% | 0.547% |
| 認証下界 | 640,000円 | 694,498.14円 |
| canonical incumbent | 650,234.73円 | 698,318.00円 |
| 判定 | 1%未達、最適性主張不可 | 1%以内で認証 |

低PV側は、燃料・電力費の独立下界が 54,498.14円となり、Gurobiの生下界が
弱くても18.36秒で1%以内を認証できた。高PV側は、日量PVを無料電源として
楽観的に使えるため、現在の独立エネルギー下界が0円となる。結果として
下界は32台×20,000円の640,000円から上がらず、初期可行解が存在しても
最適性証明だけが3600秒継続した。

この差から、高PV側の改善優先度は次の順になる。

1. 車両別に複製された便間辺を、動力別path coverまたは便鎖列へ集約する。
2. 配車masterと時刻別PV・BESS・充電recourseを、認証可能な分解法で解く。
3. 高PVでも必ず発生する充電損失、車両電池throughput、時刻不一致等を、
   二重計上せず厳密に証明できる下界へ追加する。
4. 厳密MILPと、ALNS等の300秒運用候補を別モード・別claimで比較する。

## 2026-08-14 の定式化監査と実装修正

上記比較後に `canonical_solver_result.json` を制約種別まで分解したところ、
高PV側の 1,598,973 制約のうち 1,243,440 件は、複数 duty fragment 間の
営業所リセット可否を全車両・全終端便・全始端便の組合せで先に列挙した
pairwise 制約だった。この制約群だけで全制約の約77.8%を占める。

現行実装では、同じ整数可行領域を保ったまま、この全件列挙を Gurobi の
lazy constraint callback に置き換えた。整数 incumbent に実際に現れた
fragment 終端・始端の組だけを canonical
`fragment_transition_diagnostic` で検査し、不正な組へ従来と同じ
`end + start <= 1` を追加する。後続辺は削減せず、時刻重複、営業所回送、
route-band、SOC、充電器、PV/BESS、会計の制約も変更していない。

callback が一度でも失敗した場合は solve を停止して失敗扱いにする。
また、Gurobi が同じ不正 incumbent を複数回提示する場合があることを
小規模実験で確認したため、同じ lazy row も違反のたびに再送する。
監査成果物には、一括生成行数、分離モード、MIPSOL 回数、検査組数、
ユニーク lazy cut 数、送信回数、callback error を別々に保存する。

これは厳密性を落として数百秒に見せる変更ではなく、冗長な root model
行を branch-and-cut へ移す定式化改善である。ただし、264便高PVケースで
実際に何秒短縮し、1% gapへ到達するかは、clean frozen commit からの fresh
Prepare と再実行でのみ判定する。旧 `f46f1e8` 結果を修正後コードの性能
証拠として再利用しない。

## 2026-08-14 のfresh Prepare診断結果

clean SHA `885bacb` から、同じ晴天シナリオをフロント/BFF経路でfresh
Prepareし、Phase 4を600秒に限定した診断runを実行した。これは
`research_run=false`、Rollingなしの診断であり、正式研究runではない。

| 項目 | 旧 `f46f1e8` | lazy分離 `885bacb` |
|---|---:|---:|
| Phase 4時間上限 | 3600秒 | 600秒 |
| Phase 4記録時間 | 3600.801秒 | 601.237秒 |
| 変数 | 780,112 | 780,112 |
| 制約 | 1,598,973 | 355,533 |
| explicit fragment pairwise rows | 1,243,440 | 0 |
| incumbent | 650,234.729円 | 650,234.729円 |
| certified bound | 640,000円 | 640,000円 |
| certified gap | 1.574005% | 1.574005% |
| explored nodes | 1 | 1 |

制約削減量1,243,440本は旧pairwise行数と一致し、fragment occupancy
24,600本、overlap clique 9,420本、変数数は変わらなかった。callbackは1回
呼ばれたが、採用incumbentは全使用車両が1 fragmentだったためlazy cutは
0本だった。したがって、このincumbentに対して削除した全列挙行は活動して
いなかった。

一方、目的値、下界、gapは全く改善していない。これにより、行数削減後の
主ボトルネックは可行解発見ではなく、高PV時に640,000円から上がらない
下界の強さだと絞り込めた。

観測時間比約5.99倍は高速化主張に使わない。時間上限、formal/diagnostic
区分、複数のcanonical input fingerprint、反復数が一致しないためである。
正本`output/diagnostic_lazy_fragment_20260814_885bacb/
performance_comparison.json`も`runtime_claim.status=NOT_CERTIFIED`としている。
同一入力・同一上限・同一区分の反復実験を行うまで、性能差は診断値に留める。

また、このrunではseparator監査値がcanonical plan metadataには存在したが、
当時の`solver_settings.json`への伝播が欠けていた。現在のコードで伝播を修正
したが、旧成果物は書き換えない。修正後の公開artifact契約は次のfresh runで
再検証する。

## Phase 3 IISをPhase 4の非方向的探索guidanceへ利用する

600秒診断のPhase 3候補を再監査すると、`32 BEV / 0 ICE`は未探索ではなく、
最初に評価されStage 2が1.545秒で`INFEASIBLE`を証明していた。`31/1`から
`28/4`も固定割当Stage 2では不可行で、`27/5`が最初の可行seedだった。
ただし統合Phase 4は別割当の`31/1`可行解を得ているため、これは動力構成の
不可行証明ではない。

Stage 2と統合Phase 4は別の数理定式化である。したがって、Stage 2 IISを
そのままPhase 4のhard cutへ変換する案はMITレビューで棄却した。現行コード
は、IISに含まれた割当binaryへ非方向的`BranchPriority=1`を設定するだけで
ある。値のhint、BEV/ICE bias、制約、目的項は追加しない。`TIME_LIMIT`、IIS
欠落、推定エネルギー不足はguidance対象外である。

pattern数・hash、元候補hash、対象変数数、priorityは`solver_settings.json`
および`phase4_iis_assignment_guidance_audit.json`へ記録される。小規模Gurobi
反例テストでは、Stage 2で否定された完全一致patternもPhase 4側では可行領域
に残ることを確認した。264便高PVケースの求解時間とgapへの効果はまだ再計測
していないため、文献の数百秒結果との性能比較には使用しない。

## 研究上の表示ルール

- `数百秒で可行解` と `数百秒で1%以内を証明` を同一視しない。
- 近似法の時間には、比較した厳密解の規模とgapを併記する。
- Gurobiが `OPTIMAL` でも、完全後続ネットワーク、会計、SOC、Rolling、
  provenanceの各ゲートを別々に報告する。
- 高PV側の現行結果は物理的可行候補であり、1%最適性結果ではない。

## `ecdb0b1` 600秒診断で確認した現在の律速

同一フロント/BFF経路の高PV診断では、Phase 3 seedはStage 1/2と独立物理
検証を含め96.226秒で完了し、統合Phase 4には共有予算の残り498.603秒が
割り当てられた。統合モデルのcanonical cost探索時間は348.048秒、変数
780,112、制約355,557、探索ノード1で、13 BEV/19 ICEのseedを改善できず、
認証gapは9.542957%だった。HTTP全体はPrepare・帳票生成を含め630.538秒で
完了した。

したがって、現在の「終わらない」問題は外側の時間上限無視ではなく、完全
後続ネットワークを車両ID別に複製したroot modelで改善incumbentを作れない
ことに移った。文献No06のGurobiも50便で617.6秒、200/418便で6時間以内に
可行解なしであり、今回の結果は厳密統合MILPの難しさと矛盾しない。一方、
固定配車または近似法の数百秒結果を、この780,112変数の厳密モデルの1%証明
時間と同列には扱わない。

次の実装は可行領域を削らず、既存のfixed-assignment recourseを用いて、全
active ICE dutyを未使用BEVへ一括置換した候補を最初に1件だけ検証する。
物理・会計とも可行で実費が厳密に改善する場合だけMIP startへ採用し、最終
Phase 4は全構成を引き続き探索する。これは高PV時の上界を早く得るための
候補順序であり、最適性証明、BEV下限制約、weather biasではない。効果は
clean commitからの再実行前には主張しない。

## `4f6a808` 再診断と候補探索の重複除去

clean commit `4f6a808` のfresh frontend/BFF診断では、共有600秒予算に対する
solver wall timeは605.836秒、HTTP全体は631.299秒だった。候補生成は、単一
ICE dutyを未使用BEVへ置換した可行候補を発見し、seedを13 BEV/19 ICE、
44/220便から14/18、60/204便へ改善した。canonical costは5,146.266645円低い
702,371.885683円となり、会計・CO2を含むdata-flow監査は失敗0件だった。
全ICE dutyの一括置換は固定割当Stage 2で不可行だった。

ただし、統合Phase 4は1 nodeに留まり、14/18 seedを改善せず、certified gap
8.880180%で終了した。したがって、この結果も「数百秒で可行な改善上界を得た」
証拠であり、「数百秒で1%以内を証明した」結果ではない。

候補監査では64件の評価上限を使い切ったが、未使用BEVは同一性能であり、同じ
ICE dutyを車両IDだけ変えて繰り返し解いていた。現在の実装は、車種、営業所、
初期SOC、容量・予備量、消費率、固定費、充電出力、対応充電器まで一致する既存
の厳密symmetry signatureで未使用BEVを同値類に分け、各類の代表1台だけを
fixed-assignment Stage 2で評価する。代表edgeが可行な場合、matching用の隣接
関係だけを厳密clone IDへ展開する。matchingで得た複数台置換は改めてStage 2、
独立物理検証、canonical cost評価を通るため、推定だけで採用されない。

これはNo06等の近似解法を導入したものではなく、同一候補の重複評価を除くMIP
start生成の効率化である。最終Phase 4の変数、制約、可行領域、目的関数は変え
ない。clean commitから同条件を再実行するまで、時間短縮やgap改善は未確認と
して扱う。

## `9db438a` 実測で判明したcandidate capの律速

`9db438a` のfresh診断はsolver wall 605.912秒、HTTP全体630.574秒で完了した。
会計・物理検証は通ったが、解は14 BEV/18 ICE、60/204便、702,371.885683円、
certified gap 8.880180%で前回と同じだった。

厳密clone代表化の推定edgeは0件だった。未使用BEV 22台は初期SOCが約21.9%から
75.1%まで異なり、全てsingleton classである。初期SOCは出発時可用エネルギーと
充電時刻へ影響するため、数百秒へ合わせる目的で同一視してはならない。

一方、探索順には実装上の無駄が残っていた。source-major順で63件のpairwise
評価を行ったため、19本のactive ICE dutyのうち3本しか調べず、matching size 3
を得た時点で64件上限へ到達した。combined candidateは未評価だった。さらに、
combinedが不可行な場合のcumulative fallbackは、最初のsingle edgeをduplicate
として捨てるため2本目以降へ進めないバグを含んでいた。

現在はICE dutyをround-robinで一巡し、target classをdutyごとに回転させる。
またmatching全体1件と各cumulative prefixを再検証できるevaluation数およびwall
timeを先に予約する。最初のprefixだけは既にStage 2・物理・会計を通ったsingle
edgeの証明を再利用し、2本以上の組合せは必ず新たに解く。この変更も最終MILPの
可行領域・目的関数を変えない。次のclean再実行までは性能改善未確認である。

## `fb72281` の600秒結果

round-robin修正後のclean診断では、19本全てのICE dutyがpairwise探索対象となり、
16本で少なくとも1台の未使用BEVへの置換が可行だった。size 16のmaximum
matchingを固定割当Stage 2で再求解すると0.921秒で可行となり、29 BEV/3 ICE、
655,689.265969円を得た。さらにsuffix exchangeで30/2、232/32便、
650,542.999324円へ改善した。

solver wallは606.003秒、HTTP全体は630.499秒である。物理・会計検証は通った。
一方、下界は640,000円、certified gapは1.620646%であり、1%目標には届かない。
これは「600秒で大幅に良い可行上界を得た」結果であり、「600秒で1%以内を
証明した」結果ではない。No06のALNS-SA等と比較する場合も、この区別を維持する。

新しい律速はsuffix local searchのanchor更新である。round 1の最初の候補で既に
30/2へ改善していたが、同じroundの別ID・別split候補をさらに14件評価して60秒を
使い、設定済みround 2へ入らなかった。現在は最初のstrict-cost improvement後に
8件だけ比較し、その範囲の最良可行候補を次roundのanchorにする。これは局所探索
順序の変更であり、Phase 4厳密モデルの定式化やgap定義は変えない。効果は次の
clean 600秒runで検証する。

## `6755213` の600秒結果と最終suffix round

再始動を入れたclean診断では、round 1が9候補を評価して5件のstrict-cost改善を
得た。改善済みanchorからのround 2は6候補を評価し、3件改善した。最終的な
validated MIP startは31 BEV/1 ICE、248/16便、648,332.208836円である。前の30/2
startより2,210.790488円、元の13/19 Phase 3 seedより59,185.943491円改善した。

solver wallは606.092秒、HTTP全体は631.631秒で、物理・会計・成果物・data-flow
検証は通った。残るICE仕業は渋23の16便、149.109944 kmである。ただし下界
640,000円に対するcertified gapは1.285176%であり、この結果は600秒内の良い
可行上界であって、1%以内を証明した結果ではない。

次の検証ではPhase 4全体600秒とseed neighborhood合計120秒を維持し、固定仕業
探索を75秒、route-band repartitionを45秒とする。suffixは最大3 round、最初の
改善後の比較猶予はround 1で8件、round 2で4件とし、最終roundへ到達できる余地を
作る。候補上限64件は変えない。これはNo06のALNS-SAを実装したものではなく、
既存の固定割当recourseをどの順序でMIP startとして試すかという限定的変更である。
BEV使用を強制せず、最終MILPの可行領域・目的関数・gap判定も変更しない。

## `ac0115e` の高PV・低PV実測と「数百秒」の意味

最終suffix roundを入れたclean診断では、frontend/BFF全体のwall timeは高PV
631.746秒、低PV630.879秒だった。最終Gurobi solveはそれぞれ272.216秒と
293.816秒であり、Prepare、Phase 3 seed、固定割当recourse、Phase 4モデル構築、
成果物生成を含む全体が約10.5分である。

したがって、利用者の観察どおり「数百秒」という桁には到達している。ただし、
高PVのcertified gapは1.285176%、低PVは1.094658%で、目標1%の証明には届かなかった。
この結果を、No16の固定配車・充電/ESS 275便1.5秒、No61の固定充電/PV/ESS
34--147秒、No06のALNS-SA 418便202.3秒と同じ厳密性として比較してはならない。
最も近いNo06のGurobi統合モデルも50便617.6秒、200/418便は6時間以内に可行解なし
である。今回の約630秒は異常な無限実行ではない一方、「文献と同様に最適化が
完了した」とする証拠でもない。

今回改善したのは、同じ600秒内に得られる可行上界とPV応答である。高PVは31/1、
248/16便、低PVは14/18、60/204便となり、両方の物理・会計検証を通過した。
今後の性能比較では、少なくとも問題規模、統合範囲、厳密/近似、gap、計算機、
Rollingの有無を併記し、wall timeだけを横並びにしない。

## `8066330` の正式低PV 3600秒実測

clean commit `80663305863a31cee1c90c5ffea6ce88eaab16b3` から、低PVケースを
fresh Prepareし、正式Phase 4と24時間Rollingをフロント相当のHTTP経路で実行した。
入力は264便、60台、可行後続辺11,310本、Gurobi 4 threads、seed 42、PV定格
1,000 kW、BESS 6,000 kWh / 900 kW、3,000 -> 3,000 kWh、従量30円/kWh、
需要料金0円、使用車両日費20,000円、要求gap 1%である。

end-to-end wall timeは3,804.389秒、Phase 4共有wall timeは3,606.030秒だった。
precheckと検証済みstart生成後に統合solveへ残った予算は3,042.031秒、記録された
solve timeは2,885.321秒である。可行warm startは0秒時点から存在した。最終割当は
14 BEV / 18 ICE、60 / 204便、全264便充足であり、独立物理検証、Rolling 24/24、
executed-day accounting、費用照合、240/240成果物hashをすべて通過した。

一方、Gurobi raw gapは8.855884%、独立下界を含むcertified gapは1.094658%で、
1%目標へ0.094658ポイント届かなかった。600秒診断 `ac0115e` と最終incumbent・
certified gapが同じであり、長い追加時間は上界改善にも1%証明にも結び付かなかった。
これは無限ループではなく、根ノード下界と現在の独立下界が律速した正式な
`TIME_LIMIT` 結果である。

したがって、先行文献の「数百秒」と比較する際は次を分離する。

1. 現行の600秒級探索は高品質な物理可行候補を得る実用経路として評価する。
2. 1%以下の厳密証明は別のgap-certification課題として扱う。
3. ALNS・分解法を追加する場合は `NEAR_OPTIMAL` と表示し、同じcanonical会計と
   物理検証を通す。
4. exact Phase 4は完全後続ネットワークを維持し、時間短縮のために後続辺、SOC、
   充電器、会計、provenanceのゲートを緩めない。
