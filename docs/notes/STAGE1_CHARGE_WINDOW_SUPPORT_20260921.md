# 配車の探索停滞と充電可能時間の等価な表現変更

## 5月2条件の確認結果

固定 `4f9799825ed8af0217575c9ef94a78b90f4137e3` は2026-09-21 09:31 JSTに2条件の診断を終了。
completion、全体summary、各条件summary、各5原本のSHAとclean状態を照合した。
両モデルのnative fingerprintは `0x426bb2e8` で、6,767,874変数、1,060,138制約、
81,379,204非零係数を持つ。NoRel以外の設定は共通。照合記録は
`output/stage1_input_binding_20260921/review.json`。

| 指標 | 双対単体法・NoRelなし | 双対単体法・NoRelあり |
|---|---:|---:|
| Stage 1目的値 | 4,297,021.92円 | 4,297,021.92円 |
| 最初の解からの費用改善 | 0円 | 0円 |
| native下界 | 0円 | 4,000,000円 |
| 認証下界（解析下界を含む） | 4,000,000円 | 4,000,000円 |
| 認証gap／目標 | 6.91227%／1% | 6.91227%／1% |
| Stage 1終了／時間 | time_limit／601.01秒 | memory_limit／225.24秒 |
| 根LPのOPTIMAL MIPNODE記録 | なし | なし |
| Gurobi使用量：開始前→終了後 | 2.768→9.951 GB | 2.768→9.814 GB |
| Gurobi最大使用量 | 16.218 GB | 19.882 GB |
| Stage 2 gap／目標 | 0.36796%／1% | 0.36796%／1% |
| 物理検証 | 通過 | 通過 |

Gurobi使用量はenvironment全体の10^9 byte単位で、Python全体のRSSではない。
SoftMemLimit=18 GBは停止可能な箇所で判定するため、NoRelの記録は上限を超えている。
双対単体法は根LPで485,636反復を行ったが未完了。NoRelも解の費用を改善していない。
下界が上がったことを費用削減と扱わない。Stage 2は固定配車の充電部分であり、
その目的値をStage 1総目的値から引いた値も改善額ではない。
実績rolling・週間費用削減・全12週・統合大域最適性は今回の証拠に含まれない。

## 次の変更と数学的な範囲

既存のStage 1は、ある開始・終了・便間接続を選択したときの充電可能区間を、
各15分slotの制約へ繰り返し列挙している。完全な候補接続は維持し、この繰返しだけを
端点の差分による連続状態へ置き換えるopt-in実装を追加した。

元の充電可能量の上界を `S[t] = Σ c[t,j] x[j]` とする。
`d[t,j] = c[t,j] - c[t-1,j]`、`S[-1]=0` と定義すると、
`S[t] = S[t-1] + Σ d[t,j] x[j]` で同じ値を表せる。
長い連続区間は開始・終了にだけ係数が現れる。
集合に穴がある場合や同じ機会が重複する場合も、元の係数の個数を保つ。
slotが連番でない場合も、元の有効slotの順序で同じ係数を再現する。

新状態に1などの上界を追加しない。元の非負の機会の和なので下界0だけを持ち、
充電可否の既存 `[0,1]` 上限はそのまま。元の係数が非負であることから、
整数解だけでなく分数割当にも同じ射影を持つ拡張定式化である。
既存の因子化接続のsupportは既に端点で表現されており、そのまま加算する。
料金・目的関数・SOC・回送時間・車両・operator・距離・時刻表・候補接続は変更しない。
既定動作は旧方式のままで、`stage1_sparse_charge_window_support=true` の診断だけで使う。

変更経路: `run_stage1_root_search_diagnosis` → 新規Prepare/検査 → `solve_week` →
`GurobiMILPAdapter._add_stage1_time_indexed_soc_relaxation` → Stage1 → Stage2 →
独立物理検証 → quality/summary。supportの出現数・端点数、nativeモデルサイズとメモリを記録する。

## 検証と次の比較

関連83 tests通過。重複・穴・不連続slot・0/分数/1の全243パターンで差分の和が元と一致した。
200slot・40変数の連続モデルで最適目的値が一致し、非零係数は元の1/5未満。
2日間のnative Stage1/2でも両表現の目的値と独立物理検証が一致した。
これは局所的な等価性・構造の検証であり、実際の5月問題の速度改善を証明しない。

[新設計](../../config/shibu21_23_stage1_support_diagnosis_20260921.json)では、新しいclean固定版から
旧dense表現と新しい端点表現の両方を新規Prepareで比較する。
両方とも双対単体法、NoRel=0、4 threads、18 GB、seed42、Stage1 600秒／Stage2 120秒、gap1%。
変えるのはsupport表現だけ。旧実行の解は比較結果やwarm startとして再利用しない。
presolveが表現を再展開する可能性や、根LPの縮退が残る可能性もあり、効果は未確定。
モデルサイズの削減、初期解からの費用改善、下界／gap、物理検証を別々に判定する。
通常処理はスクリプトに任せ、終了時だけ既存タスクへ1回通知する。全12週・メール送信は行わない。

自己レビューと独立研究レビューは別。独立レビューPENDING、研究採用BLOCKEDを維持する。

全体回帰: 2442 passed／既存PowerPoint関連2 failed（108.09秒）。前回と同じ資料hash・部品構成の
不一致で、資料と期待値は変更していない。最適化関連の新規失敗は0件。ログは
`output/stage1_support_regression_20260921.log`。自己レビューの新規P0/P1残件0。
