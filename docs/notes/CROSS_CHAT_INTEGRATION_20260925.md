# 他チャットの統合基準（2026-09-25）

## 統合対象

- 最新main cb028fa8を出発点とし、期間別計画、12代表週、SOC出庫前判定、同一attempt回復、手動ODPT固定DBを維持。
- C:/master-course の未コミット変更を9a2c8f91として保全。営業所→系統→運行パターンの閲覧、ODPT/GTFS/シナリオの出典分離、公式参考所管と未確認の表示、テスト・点検資料を統合。
- 旧取得ブランチe9073e77の機能は既存mainの78baf66aと後続変更に収録済み。旧版へ戻さない。旧段階試験専用のAI終端通知ブランチは現行のweekly_terminal_observer/weekly_operatorに置換済みであり、自動起動を追加しない。
- 011c0bd6 / 0ef5f301 / 7441f417の画面改善は前回mainに統合済み。別監視先のデータ混同防止を維持。
- 未追跡の旧verified_stage_config.pyは、mainのmanifest/probe/dataset照合付き実装より古い。原本をoutput/chat-integration-20260925へ保全し、新版を採用。
- Solcast取得原本3件はローカルに保持。この統合では外部API取得・原本公開・資格情報の追加は行わない。

## 自己レビューと修正

P1（修正済み）: 営業所を追加すると、既存営業所で個別解除した路線まで復活する。追加営業所のパターンだけを追加し、所管不明の選択IDも黙って削除しない。回帰テストで追加/解除の両方向を検証。

P1（修正済み）: 表示用距離の刷新で既存DB取込が呼ぶ_straight_line_distance_kmが消え、手動ODPT→SQLite生成がNameErrorで停止。取込側の既存契約を保持して復旧。表示用の隣接停留所距離を最適化入力へ転用しない。

競合解消: 日数プリセット・期間終端表示と新路線ブラウザーを両立。所管未確認を含む従来の全路線検索も残す。READMEの古い4季節だけの記述を現行12か月の計画へ上書きしない。

## 検証範囲

- Python関連70件通過（desktop編集・大規模一覧・端末配置証拠・手動DB構築・路線・期間・分類）。
- フロント全57件通過。TypeScript・本番ビルド通過。
- 初回のPython2件失敗は上記NameErrorを検出したもの。修正後に同じ70件を再実行して通過。
- 独立レビュー、統合版の新規実規模7日間、全PCへの新版配布は本統合の合格を意味しない。稼働中の固定求解コードへ上書きしない。
- GitHub Actionsはworkflow_dispatchのみ。自動CIや有料サービスを有効化しない。

mainは開発・正式版コードの共通基準。研究結果の正式採用、最適性、実営業所への設備適合の承認とは別である。
