# Oracle figure specification

Source only: each immutable `oracle_result.json`. X axis is 8, 12, 24 trips. Show absolute P3-minus-P4 actual-cost distance and runtime in separate panels. A missing exact gate produces a labelled gap, not zero. Caption must say deterministic subset and prohibit 264-trip approximation interpretation.
